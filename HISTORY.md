# История проекта ARDU

Хронология хранит только фактически выполненные действия и полученные данные. Старые записи не переписывать по смыслу.

## 2026-09-05 — инициализация репозитория

- Создан репозиторий `dronrome1245/ARDU`.
- Зафиксирована цель: круглая потолочная лампа со светомузыкой, микрофоном и управлением со смартфона.
- Проанализированы старые материалы по Arduino/HC-05/MAX9814/DS3231/WS2812B и ColorMusic.
- Выбрано рабочее направление Nano + ESP8266.
- Созданы базовые документы архитектуры, требований, протокола и дорожной карты.

## 2026-09-05 — переход к инженерной модели ведения проекта

- В качестве образца организации изучен репозиторий `dronrome1245/RC-Toy-Fighter`.
- Принято вести ARDU с постоянным контекстом для любой нейросети: `00_AI_START_HERE.md`, `STATUS.md`, `DECISIONS.md`, `HISTORY.md`, `SOURCES.md`.
- Принято сначала инвентаризировать все имеющиеся компоненты и только потом утверждать закупки.

## 2026-09-05 — первая расширенная фотоинвентаризация

Пользователь предоставил серию фотографий имеющейся электроники.

По фото зафиксированы:

- набор выводных LED разных цветов;
- HC-SR04;
- релейный модуль 5 В SRD-05VDC-SL-C;
- потенциометр B10K;
- LCD МЭЛТ MT-16S2H 16×2;
- набор электролитических/керамических конденсаторов;
- I2C LCD backpack на PCF8574/совместимом чипе;
- W5500 Ethernet-модуль;
- зуммер неизвестного типа;
- USB A ↔ Mini-B кабель;
- Arduino Nano ATmega328P с Mini-USB;
- Arduino Nano ATmega328P-совместимая плата с USB-C;
- YP-8 на AMS1117-3.3;
- отдельная PCF8574A DIP;
- маркировка реле TIANBO HJR1-2C L-12V;
- полноразмерная макетная плата.

Несколько фото относятся к разным сторонам/крупным планам одних и тех же плат и не считаются отдельными экземплярами.

## 2026-09-05 — вторая фотоинвентаризация

Пользователь предоставил ещё одну серию фотографий, включая крупные планы маркировки ранее известных и новых компонентов.

Подтверждено:

- RTC-модуль DS3231 с AT24C32/совместимой EEPROM и батарейным держателем; установлен CR2032;
- MAX9814 — основной микрофонный модуль, маркировка и выводы читаются на фото;
- MAX4466 — отдельный микрофонный усилитель с ручной регулировкой gain;
- HC-05 физически присутствует; сфотографированы обе стороны;
- БП S-30-5 физически присутствует; по шильдику 5 В, 6 А, 30 Вт;
- электролитические конденсаторы 1 мкФ — 10 шт.;
- электролитические конденсаторы 100 мкФ — 3 шт.;
- BC548 — 10 шт.;
- 1N4148 — 10 шт.;
- резисторы 1,5 кОм — 10 шт.;
- резисторы 4,7 кОм — 10 шт.;
- на фоне видны соединительные провода Dupont/аналогичные, но их типы и количество пока не подсчитаны.

Дополнительный крупный план Mini-USB Nano не был автоматически принят за новый отдельный экземпляр.

После этой партии BOM уточнён: новый микрофон, RTC и отдельный level shifter покупать пока не требуется. Для линии Nano TX → ESP8266 RX зафиксирован кандидат делителя из трёх имеющихся резисторов 1,5 кОм с обязательной проверкой мультиметром до подключения.

Отдельным вопросом безопасности отмечена CR2032 в RTC-модуле: до питания проверить, нет ли на плате цепи её подзаряда.

## 2026-09-06 — инвентаризация закрыта

- Пользователь подтвердил, что дополнительных компонентов больше нет.
- Этап сбора списка наличия завершён.
- Проект переведён к проверке совместимости, питания, геометрии лампы и оставшихся инженерных неопределённостей.

## 2026-09-06 — определена конфигурация двух круглых светильников

Пользователь уточнил фактическую схему комнаты и назначение корпусов:

- первая клавиша выключателя управляет отдельными точечными светильниками и остаётся основным белым светом;
- два круглых плоских светильника больше не ремонтируются как белые лампы;
- оба корпуса используются под WS2812B для светомузыки и светового будильника;
- вторая клавиша должна полностью снимать питание со всей ARDU-системы;
- требуется один общий БП и синхронная работа обоих колец;
- мобильное приложение пишется заново;
- фактический отрезок 72,5 см содержит 44 LED, кантик корпуса 71,5 см.

По этим данным:

- целевая плотность ленты принята около 60 LED/м;
- проектная цель — 43 LED на корпус, 86 LED суммарно, с физической проверкой перед резкой;
- выбран один Nano/ESP/MAX9814/DS3231 на оба корпуса;
- два кольца получают отдельные DATA-выходы D6 и D7 и синхронный кадр;
- Wi‑Fi выбран: Station основной + SoftAP резерв; первая версия приложения работает через локальный HTTP API;
- белая ESP8266-переходная плата идентифицирована по схеме `103/0/103` как вариант без установленного стабилизатора, поэтому внешний VCC зафиксирован как 3,3 В;
- для S-30-5 установлен предварительный программный лимит лент около 4,5 А;
- на новом фото подтверждён электролит `100 µF 10 V`;
- дисковый `103` идентифицирован как 10 нФ;
- по фото DS3231 подтверждены `201` и стеклянный диод зарядной цепи; для CR2032 принято отключить зарядную цепь перед штатным питанием.

Создан подробный файл `02_Архитектура_и_решения/ПИТАНИЕ_И_ДВА_СВЕТИЛЬНИКА.md` с топологией питания, измерениями и вопросами физического монтажа.

## 2026-09-06 — уточнён запас ленты и источник приложения

- Пользователь подтвердил наличие **5 м WS2812B**; дополнительную ленту для двух колец покупать не требуется.
- Пользователь сообщил, что предыдущий мобильный прототип собирался в **MIT App Inventor** (`ai2.appinventor.mit.edu`).
- Для анализа предыдущего приложения принято получить исходный проект в формате `.aia`; `.apk` не является заменой исходникам блоков.
- После получения `.aia` нужно разобрать экраны, блоки, старые Bluetooth-команды и полезные UX-решения, затем уже проектировать Wi‑Fi-версию.

## 2026-09-06 — разобран Ardublock2.aia и спроектирована функциональная модель v1

- Получен и распакован `Ardublock2.aia`; внутреннее имя проекта — `LumpV3.1`.
- Подтверждены пять экранов: `Screen1`, `Obichniy`, `Nochnik`, `FON`, `Disco`.
- В `Disco` подтверждены 9 режимов ColorMusic, подрежимы и именованные параметры.
- Восстановлен фактический старый Bluetooth-протокол `Disco` и `Obichniy`.
- Установлено, что ранее проектные команды `L1/MM/...` не были фактическим протоколом этого `.aia`; документ протокола исправлен.
- Установлено, что `Nochnik` и `FON` в загруженной версии имели интерфейс, но их прикладные блоки управления не были завершены.
- Из ColorMusic отдельно зафиксирована таблица режимов и параметров.
- Определён целевой функциональный набор ARDU v1: Светомузыка / Фон / Ночник / Будильник.
- Цветная подсветка ColorMusic перенесена из музыкальных режимов в раздел фоновых эффектов.
- Литеральная цветовая температура Kelvin исключена из основной модели WS2812B; вместо неё используются цвет/HUE, насыщенность и яркость.
- Спроектирован новый локальный HTTP API v1 и модель экранов нового приложения.
- Создан поэтапный порядок реализации прошивки от одного кольца и постоянного цвета до FHT, RTC и полного набора режимов.

## 2026-09-13 — первичная проверка YP-8

- YP-8 подключён к 5-вольтовой линии от S-30-5 через макетную плату.
- Без подключённой нагрузки на выходе YP-8 мультиметром измерено `3,31 В`.
- Первичная проверка стабилизатора без нагрузки пройдена.
- Нагрузочная проверка с ESP8266 и активным Wi‑Fi ещё не выполнена.

## 2026-09-13 — первичная проверка S-30-5

- На низковольтной линии `V+ / V-` фактического S-30-5 мультиметром измерено `5,01 В`.
- В момент измерения существенной рабочей нагрузки не было; YP-8 был подключён без нагрузки на своём выходе.
- Выход 5 В без существенной нагрузки соответствует номиналу.
- Проверка S-30-5 под нагрузкой и контроль нагрева ещё не выполнены.

## 2026-09-13 — YP-8 с подключённым ESP8266

- К выходу YP-8 подключена фактическая белая плата ESP8266.
- На питании ESP8266 мультиметром измерено `3,28 В` против `3,31 В` без нагрузки.
- Наблюдаемая статическая просадка составила около `0,03 В`; базовое питание ESP по напряжению выглядит стабильным.
- Активность Wi‑Fi, отсутствие перезагрузок при радиопередаче и нагрев AMS1117 в этом измерении не подтверждены; это следующий отдельный тест.


## 2026-09-18 — повторная проверка питания ESP8266

- На проводах питания от YP-8 без подключённого ESP8266 измерено `3,32 В`.
- После подключения фактического ESP8266 напряжение непосредственно между его `VCC` и `GND` измерено около `2,1 В`.
- Это противоречит предыдущему измерению `3,28 В` с подключённым ESP8266 от 2026-09-13; прежний вывод о стабильном питании считать неподтверждённым до повторной диагностики.
- Следующая проверка: при подключённом ESP8266 измерить напряжение непосредственно на `VOUT` и `GND` YP-8, чтобы отделить просадку самого стабилизатора/источника от падения на проводах или контактах.


## 2026-09-19 — loopback-тест Nano как USB↔UART-моста

- Nano подключена к компьютеру по USB; ATmega328P удерживался в reset соединением `RESET → GND`.
- Для loopback соединены `D0/RX ↔ D1/TX`.
- В Serial Monitor на `115200` отправленный тестовый текст корректно вернулся обратно.
- USB-UART часть Nano подтверждена как рабочая для дальнейшего использования в роли моста.
- Это ещё не подтверждает успешную прошивку ESP8266: перед подключением UART к ESP сначала требуется устранить/локализовать просадку питания до около `2,1 В`, зафиксированную 2026-09-18.


## 2026-09-19 — локализация падения питания ESP8266

- При подключённом ESP8266 измерено: `VIN YP-8 = 4,7 В`, `VOUT YP-8 = 3,30 В`, `VCC ESP8266 = 3,10 В`.
- YP-8 при этой нагрузке сохраняет около 3,3 В на собственном выходе.
- Между выходом YP-8 и питанием ESP наблюдается падение около `0,20 В`, что указывает на сопротивление/плохой контакт в проводах, макетке или соединениях.
- На входе YP-8 также наблюдается около `4,7 В`, что ниже ранее измеренных `5,01 В` без существенной нагрузки; следующий тест — измерение непосредственно на клеммах S-30-5 при той же нагрузке.
- До стабилизации около 3,3 В непосредственно на ESP прошивку и Wi‑Fi-тест не начинать.


## 2026-09-19 — S-30-5 под текущей нагрузкой ESP8266

- При подключённом через YP-8 ESP8266 напряжение непосредственно на клеммах `V+ / V-` S-30-5 измерено `5,00 В`.
- Одновременно ранее были получены `VIN YP-8 = 4,7 В`, `VOUT YP-8 = 3,30 В`, `VCC ESP = 3,10 В`.
- Следовательно, S-30-5 удерживает номинальное напряжение под этой небольшой нагрузкой, а заметные падения возникают в соединениях/контактах между БП, YP-8 и ESP8266.


## 2026-09-20 — измерения относительно общей точки S-30-5 V-

- При подключённом ESP8266 и неподвижном чёрном щупе на `S-30-5 V-` получены: `S-30-5 V+ ≈ 5,0 В`, `YP-8 VIN+ ≈ 4,7 В`, `YP-8 VOUT+ ≈ 3,50 В`, `ESP VCC ≈ 3,25 В`.
- Эти значения измерены относительно общей точки БП, а не относительно локальных GND YP-8/ESP.
- Значение `3,50 В` на `YP-8 VOUT+` не считать истинным выходом стабилизатора до измерения `YP-8 GND` относительно той же точки; оно может включать подъём локального GND из-за сопротивления обратного проводника.
- Следующий тест: не двигая чёрный щуп с `S-30-5 V-`, измерить `YP-8 GND` и `ESP GND`.


## 2026-09-20 — подтверждено большое падение по GND-тракту

- При неизменном чёрном щупе на `S-30-5 V-` измерено: `YP-8 GND = 0,8 В`, `ESP GND = 1,5 В`.
- Эти значения ненормальны для общего низковольтного GND и подтверждают высокое сопротивление/плохой контакт в обратном проводнике или макеточной разводке.
- С учётом ранее измеренных относительно той же точки `YP-8 VIN+ ≈ 4,7 В`, `YP-8 VOUT+ ≈ 3,50 В`, `ESP VCC ≈ 3,25 В`, фактические локальные напряжения оказываются сильно заниженными.
- Решение следующего шага: обесточить систему и пересобрать питание короткими прямыми соединениями, исключив макеточную шину/сомнительные Dupont-контакты из силового пути ESP8266.


## 2026-09-20 — питание ESP8266 стабилизировано после пересборки

- После обнаружения большого падения по GND-тракту низковольтное питание пересобрано прямыми короткими соединениями, исключив проблемную макеточную/контактную разводку из силового пути ESP8266.
- Контрольные локальные измерения: `S-30-5 = 5,0 В`, `YP-8 VIN = 5,0 В`, `YP-8 VOUT = 3,3 В`, `ESP VCC = 3,3 В`.
- Базовая статическая проверка питания YP-8 → ESP8266 пройдена.
- Следующий этап: первая прошивка ESP8266 через уже проверенный Nano USB↔UART-мост, затем тест активного Wi‑Fi с контролем напряжения/нагрева/перезапусков.


## 2026-09-20 — проверка резистивного делителя Nano→ESP RX

- Все три использованных резистора по цветовой маркировке подтверждены как `1,5 кОм ±5%`.
- После исправления соединения измерено: `Nano D0/RX → узел ESP RX ≈ 1,5 кОм`; `узел ESP RX → GND ≈ 3,0 кОм` (два резистора 1,5 кОм последовательно).
- Резистивная часть делителя соответствует проектной схеме. Перед первой прошивкой остаётся проверить фактическое напряжение в узле ESP RX при UART HIGH.


## 2026-09-20 — проверка фактического уровня делителя Nano→ESP RX

- При Nano в режиме USB↔UART-моста (`RESET → GND`) измерено `Nano D0 = 4,95 В` в состоянии UART HIGH.
- В узле после делителя 1,5 кОм / 3,0 кОм измерено `ESP RX = 3,36 В`.
- На питании ESP8266 одновременно измерено `ESP VCC = 3,20 В`.
- Уровень RX соответствует требуемому 3,3-вольтовому интерфейсу; питание находится в допустимом диапазоне ESP8266, но требует контроля при прошивке и активном Wi‑Fi.


## 2026-09-20 — успешная компиляция первого скетча ESP8266

- Тестовый скетч для ESP8266 успешно собран в Arduino IDE без ошибок компиляции.
- Отчёт сборки: глобальные/статические данные RAM `28108 / 80192` байт (35%); Instruction RAM `59747 / 65536` байт (91%, включая `32768` байт зарезервированного flash instruction cache и `26979` байт IRAM-кода); код во flash `236148 / 1048576` байт (22%).
- Это подтверждает корректную установку ESP8266 Arduino core и выбранную конфигурацию платы на уровне компиляции. Загрузка бинарника в физический ESP8266 остаётся следующим тестом.


## 2026-09-20 — первая попытка записи во flash ESP8266

- Arduino IDE успешно установила соединение с физическим ESP8266 через Nano USB↔UART: определён `ESP8266EX`, crystal `26 MHz`, загружен и запущен esptool stub.
- Flash автоматически определена как `4 MB`.
- Запись началась (`Writing at 0x00000000...`) и оборвалась на первом блоке с `Failed to write compressed data to flash after seq 0 (result was C100)`.
- Факт подключения к ROM bootloader и запуска stub подтверждён; загрузка прошивки во flash пока не завершена.
- Следующий минимальный тест: снизить Upload Speed с 115200 до 57600; при повторе ошибки — 38400 и дополнительная проверка качества UART/питания.


## 2026-09-20 — попытка прошивки ESP8266 на 57600

- После первой ошибки записи `C100` скорость загрузки снижена до `57600`.
- Новая попытка завершилась раньше: `Failed to connect to ESP8266: Timed out waiting for packet header`.
- Так как предыдущая попытка успешно определяла ESP8266EX и запускала stub, UART-тракт ранее работал; текущая ошибка указывает на отсутствие корректного входа ESP8266 в ROM bootloader при этой попытке.
- Следующий тест: удерживать `GPIO0` в LOW и выполнить аппаратный reset (`RST` кратковременно на GND, затем отпустить), после чего повторить Upload.


## 2026-09-20 — повторная запись ESP8266 на 57600

- При `Upload Speed = 57600` и принудительном входе в bootloader (`GPIO0 = LOW`, краткий reset) esptool снова успешно определил `ESP8266EX`, запустил stub и определил flash `4 MB`.
- Запись прошивки продвинулась дальше первой попытки: были записаны блоки до `15%` (`0x00000000`, затем `0x00004000`).
- После `seq 1` снова получена ошибка `Failed to write compressed data to flash ... (result was C100)`.
- Boot-режим и базовая UART-связь подтверждены; нестабильность остаётся именно во время передачи/записи. Следующий минимальный тест — `Upload Speed = 38400` без других изменений схемы.


## 2026-09-20 — ошибка 0107 после установки 100 µF

- На питание ESP8266 установлен электролит `100 µF 10 V` непосредственно между VCC и GND.
- Новая попытка прошивки при тех же базовых соединениях завершилась на этапе загрузки stub ошибкой `Failed to write to target RAM (result was 0107)` после успешного определения `ESP8266EX`.
- По документации esptool `0107` означает timeout при записи в RAM и обычно связан с нестабильным serial-соединением/USB-UART/драйвером или качеством тракта Host→ESP; сама flash-конфигурация к этой ошибке не относится.
- Следующий предпочтительный тест: укоротить UART-провода и/или проверить второй имеющийся Nano как альтернативный USB↔UART-мост.


## 2026-09-20 — уточнён штатный 1 кОм в UART Nano

- Владелец указал на штатный последовательный резистор `1 кОм` между USB-UART (CH340/FT232-класс) и пином D0 Nano; для классической Nano V3.3 это также подтверждается официальной схемой Arduino (RP1A/RP1B по 1 кОм на RX/TX).
- Первоначальный расчёт внешнего делителя `1,5 кОм / 3,0 кОм` для режима Nano-as-USB↔UART этот штатный резистор не учитывал.
- Это не отменяет отдельное согласование рабочего сигнала ATmega D1/TX → ESP RX: в финальном режиме источник сигнала — ATmega, а не USB-UART через штатный 1 кОм.
- Ошибки `C100/0107` при прошивке теперь дополнительно рассматриваются как возможное следствие избыточного последовательного сопротивления/ухудшения фронтов programmer UART.


## 2026-09-20 — фотоуточнение UART-обвязки фактической Nano

- Получены крупные фото обеих сторон используемой Nano.
- Рядом с 16-выводным USB-UART-чипом на обратной стороне видны два SMD-резистора с маркировкой `151`, то есть `150 Ом`.
- На лицевой стороне около индикаторов `RX`, `TX`, `POW`, `L` видны четыре SMD `102`, то есть `1 кОм`; по расположению они относятся к индикаторной обвязке и не являются доказательством последовательного 1 кОм в UART-пути.
- Предыдущее утверждение, что на **конкретной** Nano гарантированно стоит 1 кОм между USB-UART и D0, снято как неподтверждённое. Нужно измерить связь D0 с парой `151`.


## 2026-09-20 — попытка прошивки без внешнего делителя

- Внешний делитель между Nano D0 и ESP RX был временно удалён, Upload Speed оставлен `57600`.
- ROM bootloader и stub снова успешно запустились; запись прошивки дошла до `30%` (`seq 3`) и затем снова завершилась `C100`.
- По сравнению с делителем 1,5 кОм / 3,0 кОм (ошибка около 15%) передача стала устойчивее, что указывает на влияние выходного сопротивления/фронтов UART.
- Прямое соединение признано неприемлемым для продолжения, поскольку ранее на D0 измерено около `4,95 В`, а ESP RX — 3,3-вольтовый вход.
- Следующий тест: низкоомный делитель 750 Ом / 1,5 кОм из имеющихся резисторов 1,5 кОм и проверка фактического HIGH до подключения ESP.


## 2026-09-20 — первая успешная полная прошивка ESP8266

- Перед успешной попыткой внешний UART-делитель был удалён, а непосредственно между `ESP VCC` и `ESP GND` установлен `100 µF 10 V`.
- Upload Speed установлен `115200`; при `Connecting...` выполнен ручной reset ESP (`RST → GND` кратковременно) при `GPIO0 = LOW`.
- Esptool определил `ESP8266EX`, flash `4 MB`; запись прошла от 0 до 100%, записано `269712` байт, затем получено `Hash of data verified`.
- Это первая воспроизводимая успешная запись flash физического ESP8266.
- Поскольку одновременно изменились два фактора (удалён делитель и добавлен 100 µF), корневая причина прежних `C100/0107` не считается однозначно установленной.
- Прямое `Nano D0 ≈ 4,95 В → ESP RX` не принимается как штатное решение и должно быть отключено после диагностической прошивки; безопасное согласование programmer UART остаётся отдельной задачей.


## 2026-09-20 — первый успешный запуск пользовательской прошивки ESP8266

- После успешной записи flash ESP переведён из режима прошивки в обычный boot: `GPIO0` убран с GND и подтянут HIGH через 4,7 кОм.
- Путь `Nano D0 → ESP RX` отключён; для чтения Serial оставлен `ESP TX → Nano D1` и общий GND.
- После перезапуска в Serial Monitor на `115200` получена строка `ARDU ESP8266 OK`.
- Подтверждено: flash-прошивка не только записалась, но и реально стартует на физическом ESP8266.
- Следующая проверка — активный Wi‑Fi с контролем питания/нагрева.


## 2026-09-20 — повторная попытка с безопасным делителем после успешной прошивки

- После подтверждённой успешной прошивки с локальным `100 µF 10 V` внешний делитель `1,5 кОм / 3,0 кОм` был возвращён для безопасного уровня RX.
- При сохранённом конденсаторе upload снова завершился ошибкой; конкретный код ошибки в сообщении пользователя не указан.
- Это усиливает гипотезу, что именно высокоомный programmer-path/качество соединения ухудшает UART-передачу. Следующий обратимый тест — делитель `750 Ом / 1,5 кОм`.


## 2026-09-20 — подтверждён ручной вход ESP8266 в bootloader

- На `Upload Speed = 57600` простое нажатие Upload не приводит к соединению: esptool остаётся на `Connecting...`.
- При `GPIO0 = LOW` краткий reset ESP (`RST → GND`, затем отпустить) запускает процесс загрузки.
- Вывод: текущая схема Nano USB↔UART не управляет reset/boot ESP автоматически; ручной reset является штатной процедурой стенда.


## 2026-09-20 — подтверждена повторяемая полная прошивка ESP8266

- При `Upload Speed = 115200`, локальном `100 µF 10 V`, временном прямом `Nano D0 → ESP RX` и `GPIO0 = LOW` прошивка снова проходит до `100%`.
- Для начала соединения требуется ручной краткий reset ESP: на этапе `Connecting...` замкнуть `RST → GND` и отпустить.
- Без ручного reset esptool не устанавливает соединение.
- Делитель `1,5 кОм / 3,0 кОм` при прочих близких условиях снова приводит к ошибке upload.
- Прямой 5-вольтовый путь не принимается как штатный из-за уровня около `4,95 В` на D0; следующий тест — низкоомный безопасный делитель.


## 2026-09-20 — начата реализация FW-0 Nano

- По решению владельца дальнейшая диагностика programmer-divider ESP8266 временно отложена.
- Создан `04_Прошивка/nano_fw0/nano_fw0.ino`.
- Реализованы: Serial `115200`, фиксированный входной буфер без `String`, неблокирующий разбор строк, `PING→PONG`, `STATUS`, `HELP`, структура базовых настроек и централизованная карта пинов.
- D6/D7 при старте удерживаются LOW, чтобы будущие DATA-линии WS2812B не выдавали случайных импульсов до FW-1.
- Физическая компиляция/загрузка на Nano и критерий `ARDU NANO FW0 READY` ещё не подтверждены владельцем.


## 2026-09-20 — FW-0 Nano компилируется, первая загрузка не прошла

- Компиляция `04_Прошивка/nano_fw0/nano_fw0.ino` успешна: `2514/30720` байт flash, `254/2048` байт SRAM.
- Upload завершился десятью предупреждениями `not in sync: resp=0x41` и ошибкой открытия `COM3` для programmer.
- Так как `0x41` = ASCII `A`, возможна помеха от всё ещё подключённого ESP, который ранее выводил `ARDU ESP8266 OK`; это требует проверки проводки, а не считается подтверждённой причиной.


## 2026-09-20 — ESP отключён, `resp=0x41` сохраняется

- Nano повторно прошивалась изолированно: D0/D1 свободны, `RESET→GND` снят, Serial Monitor закрыт.
- Ошибка `not in sync: resp=0x41` сохранилась без изменений.
- Помеха от ESP8266 исключена как причина этой конкретной ошибки.
- Следующий диагностический шаг — `ATmega328P (Old Bootloader)`.


## 2026-09-20 — FW-0 Nano успешно закрыт

- Для фактической Mini-USB Nano подтверждён загрузчик `ATmega328P (Old Bootloader)`; с обычным вариантом upload выдавал `not in sync: resp=0x41`.
- `nano_fw0.ino` успешно загружен.
- Команда `PING` возвращает `PONG`.
- Команда `STATUS` вернула `STATUS FW=FW0 POWER=OFF BRIGHTNESS=32 UPTIME_MS=127729`.
- Критерий FW-0 выполнен: Nano стабильно стартует и отвечает на диагностические команды.
- Создан `04_Прошивка/nano_fw1/nano_fw1.ino`: один канал WS2812B на D6, F01 постоянный цвет, яркость, ON/OFF, лимит мощности 500 мА и Serial-команды диагностики.


## 2026-09-20 — FW-1 успешно компилируется

- `nano_fw1.ino` успешно прошёл Verify/Compile на выбранной Mini-USB Nano-конфигурации.
- FastLED доступна и код одного кольца/F01 собирается без ошибок.
- Аппаратный критерий FW-1 ещё не выполнен; следующий тест — одно WS2812B-кольцо/первые 43 LED на D6 с отдельным 5 В питанием и общей землёй Nano.


## 2026-09-20 — FW-1 синхронизирован со схемой AlexGyver

- Владелец попросил придерживаться опробованных аппаратных схем исходного проекта ColorMusic/AlexGyver.
- Проверены официальные страницы AlexGyver ColorMusic и WS2812 guide.
- Подтверждено: на питание ленты ставится электролит минимум `1000 µF` (в общем гайде `1000–2200 µF`), а в DATA-линии используется отдельный последовательный резистор `200–500 Ом`.
- Временная рекомендация `1,5 кОм` в DATA для FW-1 отменена.


## 2026-09-20 — подтверждён электролит 1000 µF 6.3 V

- Владелец прочитал маркировку фактического конденсатора: `1000 µF 6.3 V`.
- Конденсатор принят для стенда FW-1 как буфер по питанию WS2812B на линии 5 В.


## 2026-09-20 — подготовлен DATA-резистор 220 Ом для FW-1

- Владелец подготовил резистор `220 Ом` для последовательной установки в линию `Nano D6 → DIN WS2812B`.
- Номинал соответствует принятому диапазону 200–500 Ом.
- Аппаратный FW-1 готов к сборке одного канала: 220 Ом в DATA + 1000 µF 6,3 V по питанию ленты.


## 2026-09-20 — FW-1 аппаратно пройден

- На одном канале D6 подключена WS2812B через `220 Ом`, питание ленты от S-30-5 с `1000 µF 6,3 V` на входе и общей землёй с Nano.
- FW-1 стартует и возвращает `STATUS FW=FW1 MODE=F01 POWER=ON BRIGHTNESS=32 RGB=32,0,0 LEDS=43 UPTIME_MS=11880`.
- Команды изменения F01 приняты с ответом `OK`; владелец подтвердил корректную работу.
- Критерий FW-1 выполнен.
- Подготовлен следующий слой `04_Прошивка/nano_fw2/nano_fw2.ino` для двух синхронных независимых DATA-каналов D6/D7.


## 2026-09-20 — физический тест второго кольца отложен

- После успешного FW-1 владелец выбрал продолжение разработки на одном кольце.
- D7 сохраняется как будущий зеркальный выход того же кадра, что D6.
- Отдельная настройка второго кольца не планируется; его аппаратная проверка будет выполнена после полной настройки первого.
- Подготовленный ранее FW-2 не удалён, но текущим этапом не является.


## 2026-09-20 — F01 одного кольца полностью проверен

- На физическом кольце D6 подтверждены `OFF`, `ON`, `BRIGHT 8`, `BRIGHT 64`, `STATUS`; владелец сообщил, что все команды работают.
- Однокольцевой F01 закрыт как воспроизводимый слой.
- Создан `04_Прошивка/nano_mic_test/nano_mic_test.ino` для следующего слоя MAX9814: A0, непрерывный сбор min/max/avg/p2p по 50-мс окнам и команда `MIC`.


## 2026-09-20 — первые реальные данные MAX9814

- MAX9814 на A0 выдаёт стабильное среднее около `249–252` ADC.
- Два 50-мс окна: `61..413, P2P=352` и `130..415, P2P=285`, по 251 отсчёту.
- DC-уровень соответствует ожидаемому смещению около 1,25 В; явного упора в границы ADC нет.
- Для корректного сравнения тишины/звука добавлен `MIC1S`, так как прежний `MIC` печатал случайное последнее 50-мс окно и мог пропускать хлопок.


## 2026-09-20 — MAX9814 уверенно реагирует на звук, обнаружен клиппинг при 60 dB

- 1-секундное окно в тишине: `AVG=252 MIN=108 MAX=417 P2P=309 SAMPLES=4998`.
- 1-секундное окно при громком звуке: `AVG=307 MIN=0 MAX=886 P2P=886 SAMPLES=4995`.
- Реакция микрофона подтверждена: амплитудный размах вырос почти в 3 раза.
- При плавающем `Gain` (60 dB) громкий сигнал достигает ADC=0, поэтому следующий тест — `Gain→GND` (50 dB).


## 2026-09-20 — ненормальный результат после Gain→GND

- После попытки установить MAX9814 в 50 dB через `Gain→GND` выход практически исчез: два 1-секундных окна дали `AVG=0`, `P2P=5` и `P2P=16`.
- Такое поведение не похоже на штатное уменьшение усиления; вероятна ошибка подключения/контакта.
- Принято сначала вернуться к ранее рабочей схеме с плавающим Gain и проверить восстановление DC-смещения около 250 ADC.


## 2026-09-20 — MAX9814 восстановился после снятия перемычки Gain

- После возврата `Gain` в floating при шуме измерено `AVG=278 MIN=0 MAX=508 P2P=508 SAMPLES=4996`.
- Работоспособность MAX9814/A0 подтверждена повторно.
- Аномальный нулевой выход возникал только при предыдущем подключении `Gain→GND`, поэтому перед новой попыткой смены усиления требуется проверить фактический вывод Gain и монтаж.


## 2026-09-20 — фото подтвердило распиновку MAX9814

- По крупному фото конкретного модуля подтверждён порядок контактов: `AR | Out | Gain | Vdd | GND`.
- Видимое рабочее подключение соответствует: Out=жёлтый, Vdd=зелёный, GND=синий; AR и Gain свободны.
- Это позволяет повторить 50 dB без неоднозначности: центральный Gain → крайний правый GND.


## 2026-09-20 — MAX9814 на 50 dB: клиппинг сохраняется

- Правильный `Gain→GND` подтвердился нормальным DC-offset: в тишине `AVG=250 MIN=85 MAX=360 P2P=275 SAMPLES=5000`.
- При громком звуке получено `AVG=260 MIN=0 MAX=507 P2P=507 SAMPLES=996`.
- Вывод: 50 dB снижает амплитуду, но громкий сигнал всё ещё достигает ADC=0. Следующий тест — 40 dB через `Gain→Vdd`.
- Отдельно замечено: во втором 1-секундном capture число samples снизилось до 996; повторно проверить на следующем тесте.


## 2026-09-20 — MAX9814, 50 dB: сравнение с лентой и без неё

- Тихое окно: `AVG=250 MIN=85 MAX=360 P2P=275 SAMPLES=5000`.
- Громкий шум при включённой ленте: `AVG=260 MIN=0 MAX=507 P2P=507`.
- Громкий шум без ленты: `AVG=257 MIN=0 MAX=506 P2P=506 SAMPLES=4991`.
- Вклад включённой WS2812B в измеряемый шум не обнаружен на уровне этого теста: амплитудные показатели практически совпадают.
- Клиппинг `MIN=0` остаётся при 50 dB; следующий тест — 40 dB (`Gain→Vdd`).


## 2026-09-20 — MAX9814 40 dB принят для M01, начат FW-5

- При `Gain→Vdd` получено: тишина `AVG=250 MIN=205 MAX=282 P2P=77`; громкий звук `AVG=250 MIN=0 MAX=503 P2P=503`.
- 40 dB даёт лучший запас по шуму среди проверенных настроек 60/50/40 dB; экстремальный хлопок всё ещё клиппует отрицательную полуволну.
- Для первого VU-режима это приемлемо: ColorMusic v2.10 оценивает максимум положительных ADC-отсчётов, а положительная амплитуда у нашего стенда уверенно отличается от тишины.
- FW-4/MAX9814 закрыт. Создан `04_Прошивка/nano_m01/nano_m01.ino` с калибровкой тишины и M01 VU-градиентом на одном 43-LED кольце.


## 2026-09-20 — M01 VU-градиент пройден на железе

- Калибровка тишины дала `QUIET_MAX=260`, `LOW_PASS=273`.
- Рабочая диагностическая строка: `PEAK=276 LOW_PASS=273 LEVEL=2.6 FILTERED=17.8 AVG=63.4 MAX=114.2 PAIRS=3`.
- Владелец подтвердил визуально корректную работу музыкального VU-градиента.
- M01/FW-5 закрыт.
- Создан минимальный следующий слой `nano_fht_test.ino` для проверки FHT и трёх частотных диапазонов до реализации M03/M04.


## 2026-09-20 — выбран точный FHT из ColorMusic вместо AvrFHT

- В Library Manager найдена только `AvrFHT`.
- Проверено: AvrFHT — модификация ArduinoFHT с изменённой архитектурой заголовков, поэтому не является прямой заменой `#include <FHT.h>` без правок.
- Для воспроизводимости с ColorMusic v2.10 выбран оригинальный каталог `libraries/FHT` из `AlexGyver/ColorMusic`.


## 2026-09-20 — FHT diagnostic переведён на AvrFHT

- Проверен репозиторий `KobaProduction/AvrFHT`; библиотека заявлена для AVR и основана на ArduinoFHT/Open Music Labs.
- Подтверждено, что AlexGyver ColorMusic действительно содержит старую FHT в `libraries/FHT`.
- `nano_fht_test.ino` адаптирован: `FHT_SAMPLES_N=64`, `fht_transform()`, `fht_mag_log()`, программное вычитание DC offset MAX9814.
- Следующий тест — установка AvrFHT через Library Manager, Verify/Compile и затем `CALF`/`FREQ`.


## 2026-09-20 — первая AvrFHT-калибровка дала ошибочный DC=914

- `CALF` выдал `DC=914 QUIET_MAX=32 SPECTR_LOW_PASS=35`; последующие `FREQ` выдавали нули.
- Значение DC=914 не согласуется с предыдущими прямыми измерениями MAX9814 (`AVG≈250`) и признано ошибкой диагностического кода/режима ADC, а не новым свойством микрофона.
- `nano_fht_test.ino` исправлен: DC offset измеряется на штатной скорости ADC, добавлена команда `ADC` для прямой проверки A0 до FHT.


## 2026-09-20 — AvrFHT: аномальный DC сохраняется после первого исправления

- Повторный `CALF` дал `DC=946 QUIET_MAX=33 SPECTR_LOW_PASS=36`.
- Два `FREQRAW` дали максимум 27 и 19.
- До интерпретации спектра требуется выполнить команду `ADC`: если она неизвестна — прошита старая версия; если работает и показывает ≈250 — ошибка остаётся внутри CALF/FHT-кода; если показывает ≈946 — проверяется физический OUT MAX9814/A0.


## 2026-09-21 — FHT остановлен из-за нулевого входа A0

- После повторной проверки Gain получено `CALF DC=0`, `QUIET_MAX=19`; `FREQRAW` дважды полностью нулевой.
- Это противоречит ранее подтверждённому MAX9814 DC bias около 250 ADC и указывает на проблему до спектрального анализа.
- Следующий минимальный тест: команда `ADC`; при нуле — снять только перемычку Gain→Vdd и повторить `ADC`, сохранив `Out→A0`.


## 2026-09-21 — обнаружена старая версия nano_fht_test на физической Nano

- Команда `ADC` вернула `ERR UNKNOWN_COMMAND`.
- Актуальный файл в репозитории содержит обработчик `ADC`, значит последняя исправленная версия фактически не была загружена на Nano.
- Предыдущие `CALF DC=0` / `FREQRAW=0` не интерпретировать до перепрошивки.


## 2026-09-21 — ADC-команда подтверждает почти нулевой сигнал A0

- После гарантированной загрузки текущего FHT-скетча `HELP` содержит `ADC`.
- `ADC AVG=0 MIN=0 MAX=13`.
- Это исключает FHT как первичную причину нулевого спектра: сначала требуется восстановить физический аналоговый выход MAX9814/A0.


## 2026-09-21 — MAX9814 подтверждён исправным мультиметром

- `Vdd→GND = 5,09 В`.
- `Out→GND = 1,24 В`.
- Это соответствует ожидаемому питанию и DC offset MAX9814 и локализует текущий `ADC=0` после выхода модуля: в проводе/контакте `Out→A0`, пине A0 либо дальше во входе Nano.


## 2026-09-21 — локализовано падение 1,24 В → 0,15 В на линии Out→A0

- На MAX9814 Out измерено 1,24 В.
- На Nano A0 одновременно измерено 0,15 В.
- Это подтверждает проблему соединения или ненормальную нагрузку входа A0; FHT/ADC-код дальше не диагностировать до восстановления этой линии.


## 2026-09-21 — восстановлена линия MAX9814 Out→Nano A0

- После исправления сигнального провода напряжение на Nano A0 стало `1,25 В` вместо `0,15 В`.
- Аппаратная причина нулевого ADC была в соединении Out→A0.
- Следующий тест — `ADC` текущего FHT-скетча, затем при нормальном AVG возврат к CALF/FREQ.


## 2026-09-21 — FHT-тракт впервые дал валидную реакцию на звук

- `ADC AVG=250 MIN=233 MAX=264` после исправления сигнального провода.
- `CALF DC=250 QUIET_MAX=61 SPECTR_LOW_PASS=64`.
- В тишине `FREQRAW LOW=19 MID=19 HIGH=16 PEAK=19`.
- При звуке `FREQRAW LOW=64 MID=30 HIGH=19 PEAK=64`.
- AvrFHT и программное удаление DC offset работоспособны. Полная проверка трёх диапазонов продолжается.


## 2026-09-21 — CALF переведён с абсолютного максимума на P90

- При `SPECTR_LOW_PASS=61` все `FREQ` были нулевыми, хотя `FREQRAW` показывал `PEAK=37`.
- Подтверждено, что абсолютный максимум 58 во время CALF завышает gate для текущего MAX9814.
- Реализована устойчивая к выбросам калибровка: 100 максимумов FHT-кадров, сортировка, `P90 + 3` как рабочий порог; абсолютный MAX остаётся только диагностикой.


## 2026-09-21 — повторно сверена оригинальная реализация FHT ColorMusic

- Установлено, что оригинал использует отдельный частотный вход A3 через конденсатор и абсолютный максимум +3 для spectrum noise gate.
- Текущий ARDU FHT-тест отличается аппаратным входом и опорой ADC; поэтому P90 помечен как экспериментальная адаптация, а не как перенос AlexGyver.


## 2026-09-21 — исследованы версии ColorMusic и MAX4466

- Подтверждён официальный changelog ColorMusic до v2.10.
- Найдена FireEdition — отдельная community-ветка Slenk/technotrasher с MIC/LINE и версиями до v3.1.
- Найдено прямое упоминание technotrasher о выборе MAX4466 для переработанной схемы, а также практическое обсуждение MAX4466 в FireEdition.
- Отдельной официальной AlexGyver-прошивки, явно предназначенной для MAX4466, не найдено.


## 2026-09-21 — направление возвращено к официальной поздней ColorMusic

- Владелец указал придерживаться поздних официальных AlexGyver ColorMusic.
- FireEdition/technotrasher остаются только референсом.
- Для конкретной базы сравниваются v2.7/v2.8/v2.10; основной кандидат — v2.10.


## 2026-09-21 — сохранена оригинальная FHT AlexGyver и поздние прошивки

- Полная текстовая копия `libraries/FHT/` из официального ColorMusic сохранена в ARDU вместе с примерами и лицензией; upstream pinned на `a3f94de053d431f15282c2f67681b77ab66c30ae`.
- Сохранены неизменённые `colorMusic_v2.7.ino`, `colorMusic_v2.8.ino`, `colorMusic_v2.9.ino`, `colorMusic_v2.10.ino`.
- Создан каталог исходных имён прошивок.
- `nano_fht_test.ino` переведён с AvrFHT на оригинальный API FHT ColorMusic.


## 2026-09-21 — устранён конфликт FHT `SCALE` ↔ FastLED

- Оригинальная FHT ColorMusic успешно находилась Arduino IDE, но её глобальный макрос `SCALE` конфликтовал с современным FastLED.
- `nano_fht_test.ino` получил локальный compatibility shim: `#undef SCALE` после FHT и до FastLED.
- Vendored `FHT.h` оставлен бит-в-бит без изменений.


## 2026-09-21 — оригинальная FHT успешно загружена на Nano

- Свежий `nano_fht_test.ino` с оригинальным `FHT.h` AlexGyver и `#undef SCALE` успешно скомпилирован и загружен.
- Этап совместимости legacy FHT ↔ modern FastLED закрыт.


## 2026-09-21 — original FHT runtime работает; CALF возвращён к ColorMusic v2.10

- Получено: `STATUS ... LIB=AlexGyver_FHT FHT_N=64 DC=390`.
- Получено: `ADC AVG=251 MIN=242 MAX=258`.
- Аналоговый вход исправен; ранний DC=390 признан некорректным для текущего сигнала.
- Экспериментальный P90 удалён. CALF снова реализует исходный принцип ColorMusic v2.10: абсолютный максимум 100 FHT-кадров по bins 2..31 + 3.
- Добавлена 500 ms пауза перед DC/шумовой калибровкой.


## 2026-09-21 — предыдущий DC=390 признан невалидным из-за отвалившегося A0

- Владелец уточнил, что сигнальный провод `Out→A0` отвалился во время прошлого теста.
- Поэтому `DC=390` не является валидным измерением нормального микрофонного тракта и не подтверждает проблему времени startup-калибровки.


## 2026-09-21 — штатный max+3 CALF заработал на оригинальной FHT

- `ADC AVG=265 MIN=259 MAX=272`.
- `STATUS DC=252` после старта.
- `CALF DC=264 QUIET_MAX=33 SPECTR_LOW_PASS=36`.
- `FREQRAW LOW=74 MID=82 HIGH=27 PEAK=82`.
- Это первый валидный запуск original FHT + original ColorMusic CALF после исправления A0 и перехода с AvrFHT.


## 2026-09-21 — noise gate FHT пройден

- Тишина после CALF=36: три последовательных `FREQ` дали 0/0/0.
- Звук: зафиксированы `LOW=38` и `LOW=66` выше порога 36.
- Gate работает. Следующая отдельная проверка — MID/HIGH контролируемыми тонами.


## 2026-09-21 — принято перейти с диагностического A0 на финальные A2/A3

- После сверки с ColorMusic v2.10 подтверждено, что официальный поздний код использует A2 для mono/VU и A3 для FHT через конденсатор.
- A0-only стенд сохраняет ценность как диагностика, но не переносится в финальную схему.


## 2026-09-21 — LOW/MID/HIGH подтверждены контролируемыми тонами на A0

- 2 кГц → только LOW, значения 84–92.
- 5 кГц → только MID, значение 92.
- 8 кГц → только HIGH, значения 68–78.
- На диагностическом A0 полностью подтверждены FHT, noise gate и разбиение диапазонов ColorMusic v2.10.


## 2026-09-21 — подготовлен A2/A3 diagnostic v2.10

- После полного FHT-pass на A0 создан отдельный следующий тест, чтобы не ломать подтверждённый baseline.
- Новый скетч использует A2 для VU, A3 для FHT и INTERNAL 1,1 V.
- Подготовлена физическая обвязка: разделительный C1, ~10 kΩ к GND и 10 nF A2→A3.


## 2026-09-22 — удалён ошибочно добавленный pull-down A2

- Сверка по официальной картинке схемы показала отсутствие резистора A2→GND.
- Ошибочная цепочка 2×4,7 kΩ удалена из проектной схемы и BOM.
- Сохранена точная цепь: Out→C1→A2; A2→103→A3.


## 2026-09-22 — исправлена ошибочная добавка 9,4 kΩ в аудиотракт

- После проверки официальной схемы владельца подтверждено: 2×4,7 kΩ A2→GND были ошибочно добавлены при интерпретации схемы.
- Реальная схема: Out→C1→A2 и A2→103→A3, без резистора на A2.
- 10–100 kΩ на рисунке — номинал отдельного AREF-потенциометра A0/REF/3V3.


## 2026-09-22 — финальный A2/A3 тракт впервые запущен на железе

- На физической Nano запущен `nano_audio_v210_test` с `A2` как VU и `A3` как FHT.
- `STATUS` подтвердил original FHT, `FHT_N=64` и `ADC_REF=INTERNAL_1V1`.
- Тишина: `ADC2 AVG=123 MIN=105 MAX=154`; `ADC3 AVG=745 MIN=726 MAX=779`.
- Оба ADC-канала живые и не насыщены; следующий тест — штатный `CALF` в тишине и gated `FREQ`.


## 2026-09-22 — CALF на A3 дал штатный порог

- Тишина: `QUIET_MAX=37`.
- Итоговый spectrum gate: `SPECTR_LOW_PASS=40`.
- Формула `max + 3` отработала как в ColorMusic; следующий шаг — gated `FREQ` в тишине.


## 2026-09-22 — gated FREQ в тишине на A3 пройден

- После `CALF=40` три последовательных `FREQ` дали `LOW=0 MID=0 HIGH=0 PEAK=0`.
- Noise gate на финальной A3-разводке подтверждён.
- Следующий шаг — повторить 2/5/8 кГц и подтвердить LOW/MID/HIGH.


## 2026-09-22 — регрессия 2/5/8 кГц на A3 дала нули

- На финальной A2/A3-разводке `FREQ` при 2, 5 и 8 кГц остался `0/0/0` при `LOW_PASS=40`.
- Следующий шаг переведён на `FREQRAW`, чтобы отделить слишком высокий gate от отсутствия полезного сигнала на A3.


## 2026-09-22 — FREQRAW 2 кГц на A3 совпал с quiet max

- Три раза при 2 кГц: `FREQRAW LOW=37 MID=19 HIGH=19 PEAK=37`.
- `PEAK=37` совпадает с `QUIET_MAX=37` из CALF, поэтому `LOW_PASS=40` корректно обнуляет сигнал.
- Следующая локализация: сравнить `ADC2/ADC3` под тем же тоном с тихими значениями.


## 2026-09-22 — ADC подтвердил прохождение 2 кГц через A2→103→A3

- 2 кГц: `ADC2=323 (294..418)`, `ADC3=853 (839..905)`.
- По сравнению с тишиной размах A2 вырос 49→124, A3 53→66.
- Полного обрыва A3 нет; следующий шаг — raw FHT на 5/8 кГц.


## 2026-09-22 — одинаковый raw FHT на 2/5/8 кГц; добавлена диагностика DC

- 5 и 8 кГц повторили результат 2 кГц: `LOW=37 MID=19 HIGH=19 PEAK=37`.
- Найдено отличие от пройденного A0-теста: там перед FHT вычитался `micDcOffset`; в текущем v2.10-тесте raw ADC идёт в FHT без центрирования.
- Добавлены команды `ADC3FAST` и `FREQRAWZ`; основной ColorMusic-путь не изменён.


## 2026-09-22 — ADC3FAST выявил насыщение при prescaler=32

- Под тоном: `ADC3FAST=1023/1023/1023`, `FREQRAWZ=0/0/0`.
- Причина нулевого zero-mean FHT: кадр уже полностью клиппирован до вычитания среднего.
- Сверка с официальной инструкцией AlexGyver показала, что внутренняя AREF 1.1 V при `POTENT=0` прямо обозначена как работающая не с любой громкостью.
- Следующий минимальный тест — `ADC3FAST` в тишине; затем при подтверждённом насыщении использовать имеющийся B10K для штатной внешней AREF.


## 2026-09-22 — добавлена проверка charge-up A3 на быстром ADC

- По datasheet ATmega328P источник ADC желательно держать низкоомным; текущий A3 после последовательного 10 nF не имеет явной DC-привязки.
- Community ColorMusic содержит практические варианты с 47 kΩ к GND и отдельным конденсатором на частотный вход, но это пока только внешние свидетельства, не проектное решение.
- В `nano_audio_v210_test` добавлены `ADC2FAST` и `ADC3TRACE` для сравнения A2/A3 при prescaler=32.


## 2026-09-22 — fast ADC saturation подтверждён на обоих аудиовходах

- Тишина: A2FAST `984 (831..1023)`; A3FAST `891 (768..1023)`; первые 16 быстрых A3 — все `1023`.
- Гипотеза «сломана только ветвь 103→A3» исключена.
- Подготовлен обратимый тест DC-привязки: по 9,4 kΩ от A2 и A3 к GND; не считать это штатной схемой до аппаратного результата.


## 2026-09-22 — создан отдельный сравнительный тест MAX4466

- Владелец решил проверить имеющийся MAX4466 вместо продолжения текущей диагностики MAX9814.
- Добавлен отдельный скетч и отдельная схема, не изменяющие MAX9814 baseline.
- В первом тесте OUT MAX4466 разводится на A2/A3 через две независимые ветви 9,4 kΩ; AREF=DEFAULT, DC subtraction выполняется программно для FHT.


## 2026-09-22 — первый MAX4466 ADC baseline около нуля

- STATUS: DC=7, DEFAULT 5V.
- ADC2=7 (1..12), ADC3=8 (4..12), ADC2FAST=8 (4..14), ADC3FAST=8 (4..15).
- Это исключает fast-ADC saturation, но не соответствует ожидаемому VCC/2 bias типичного MAX4466 breakout.
- Следующий тест — мультиметр прямо на VCC/GND и OUT/GND модуля.


## 2026-09-22 — MAX4466 OUT около 0 V при нормальном питании

- На модуле измерено `VCC≈5,1 V`, `OUT≈0 V`.
- До дальнейшего кода/FHT диагностика переведена на изолированный модуль: только VCC/GND, OUT полностью отключён от Nano.


## 2026-09-22 — MAX4466 исправен; внешний стенд тянул OUT вниз

- В изоляции: `VCC=5,1 V`, `OUT=2,55 V`.
- Это соответствует нормальному bias VCC/2.
- Диагностика возвращена к минимальной схеме: прямой OUT→A2 без резисторов и без A3.


## 2026-09-22 — прямой MAX4466→A2 устранил saturation, но ADC ниже ожидаемого

- Direct OUT→A2: ADC2=244 (208..293), ADC2FAST=230 (199..286).
- Fast ADC теперь нормален и близок к slow ADC.
- До FHT нужно сопоставить цифровой уровень с физическим напряжением OUT/A2 при подключённой Nano.


## 2026-09-22 — создан минимальный ADC sanity после несоответствия 2,55 V ↔ 244 counts

- OUT и A2 мультиметром одновременно = 2,55 V.
- ADC diagnostic показывает ~230–244, что несовместимо с DEFAULT ~5 V reference.
- Создан отдельный `nano_adc_sanity` без FHT и ручной настройки ADC.


## 2026-09-22 — MAX4466 не прошёл изоляционный тест

- При полностью свободном OUT и только VCC/GND измерено: `VCC≈5,1 V`, `OUT≈0,1 V`.
- Ожидаемый bias типичного MAX4466 breakout — около VCC/2.
- Сравнительный MAX4466 FHT-тест остановлен как аппаратно невалидный.


## 2026-09-22 — финальный audio frontend возвращён на рабочий A0

- Двухвходовый A2/A3 тракт ColorMusic не дал воспроизводимого частотного результата; быстрый ADC насыщался.
- Сравнительный MAX4466 не принят как основа после противоречивых аппаратных измерений; последнее измерение владельца в изоляции: OUT≈0,1 V при VCC≈5,1 V.
- Принят D-040: MAX9814 Out→A0 напрямую, DEFAULT AREF, software DC subtraction.
- Подготовлен единый диагностический скетч для VU + FHT на одном A0.


## 2026-09-23 — single-input A0 восстановлен, startup DC=467 признан невалидным

- Текущий MAX9814/A0: `AVG=237 MIN=225 MAX=250 P2P=25`.
- Startup status одновременно содержал `DC=467`, поэтому стартовая DC-калибровка не соответствует установившемуся входу.
- Для следующей проверки использовать `CALF` в тишине; в код добавлена отдельная команда `CALDC` для будущих запусков.


## 2026-09-23 — single-input CALF восстановлен

- `CALF DC=234 QUIET_MAX=33 SPECTR_LOW_PASS=36`.
- DC согласован с текущим ADC≈237; прежний startup DC=467 признан стартовым выбросом.
- Следующий шаг — gated FREQ в тишине и регрессия 2/5/8 kHz.
