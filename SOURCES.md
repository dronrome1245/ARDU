# Источники проекта ARDU

Здесь фиксируются внешние источники, пользовательские материалы и фактические измерения. Источник не превращает предположение в факт автоматически: для конкретной детали приоритет имеет маркировка фактического экземпляра и измерение.

## S-001 — AlexGyver ColorMusic

- Сайт проекта: `https://alexgyver.ru/colormusic/`
- Репозиторий исходников: `https://github.com/AlexGyver/ColorMusic`
- Назначение: режимы светомузыки, FastLED/FHT-архитектура, калибровка, сохранение настроек.
- Лицензия исходного проекта: MIT.

## S-002 — видео-референс 1

`https://www.youtube.com/watch?v=UCz8JVM37BM`

Назначение: визуальная/концептуальная идея светомузыки.

## S-003 — видео-референс 2

`https://www.youtube.com/watch?v=nu31By9Phdc`

Назначение: визуальная/концептуальная идея светомузыки.

## S-004 — старые пользовательские материалы

В текущий чат были загружены материалы по распиновке Arduino/HC-05/MAX9814/DS3231/WS2812B, ColorMusic, будильнику-рассвету, протоколу команд приложения, калибровке микрофона, тесту хлопков и конденсаторам аудиотракта.

Эти материалы рассматриваются как история предыдущей разработки. При конфликтах распиновки они не являются автоматической истиной.

## S-005 — фото ESP8266MOD AI-THINKER

Пользовательское фото 2026-09-05: модуль `ESP8266MOD`, `AI-THINKER` установлен на белой переходной плате с выведенными контактами.

На верхней стороне переходной платы видны резисторы `103`, `0`, `103`; отдельный стабилизатор на нижней стороне не установлен.

## S-006 — первая расширенная фотоинвентаризация

Пользовательские фото 2026-09-05. Зафиксированы позиции из `02_Наши_детали/ИНВЕНТАРЬ.md`.

Особо читаемые маркировки: `HC-SR04`, `SRD-05VDC-SL-C`, `B10K`, `МЭЛТ MT-16S2H v.7`, `W5500`, `AMS1117-3.3 / YP-8`, `PCF8574A`, `ATmega328P`, `TIANBO HJR1-2C L-12V`.

## S-007 — документация MT-16S2H

Производитель МЭЛТ: MT-16S2H — символьный LCD 16×2, питание 5 В, параллельный интерфейс 4/8 бит, контроллер совместимого класса KS0066/HD44780.

Источник: документация производителя `melt.com.ru`.

## S-008 — YP-8 / AMS1117-3.3

Публичные описания идентифицируют YP-8 как модуль линейного стабилизатора 3,3 В на AMS1117. Для нашего экземпляра перед использованием обязательно измерить фактический VOUT и поведение под нагрузкой.

## S-009 — вторая фотоинвентаризация 2026-09-05

Пользовательские фотографии подтвердили DS3231/CR2032, MAX9814, MAX4466, HC-05, S-30-5 5 В 6 А, электролиты 1 мкФ и 100 мкФ, BC548, 1N4148, резисторы 1,5 кОм и 4,7 кОм.

## S-010 — White Adapter board for ESP8266

Источник: `https://homeding.github.io/boards/whiteadapter.htm`

Описание совпадает с нашей белой платой по трём элементам на верхней стороне:

- левый 10 кОм (`103`) подтягивает CH_PD к VCC;
- средний `0 Ω` является перемычкой внешнего VCC непосредственно к Module-VCC при **неустановленном стабилизаторе**;
- правый 10 кОм (`103`) подтягивает GPIO15 к GND;
- GPIO0 должен быть HIGH для запуска и LOW для serial upload;
- при добавлении регулятора средний `0 Ω` должен быть удалён.

Это основание считать VCC нашей платы 3,3-вольтовым.

## S-011 — Ai-Thinker ESP-01/07/12 Series Module Manual

Источник: `https://docs.ai-thinker.com/_media/esp8266/esp8266_series_modules_user_manual_en.pdf`

Для ESP-12-серии:

- питание модуля 3,0–3,6 В, типично 3,3 В;
- boot/run: EN high, GPIO15 low, GPIO0 high, GPIO2 high;
- download: EN high, GPIO15 low, GPIO0 low, GPIO2 high.

## S-012 — DS3231 ZS-042-подобная зарядная цепь

Источник: `https://www.onetransistor.eu/2019/07/zs042-ds3231-battery-charging-circuit.html`

Для распространённой синей платы DS3231/AT24C32 описана зарядная цепь из резистора 200 Ом и 1N4148. Для обычной CR2032 рекомендуют отключить заряд, удалив резистор или диод. На нашем фото видны резистор `201` и стеклянный диод, соответствующие этой цепи.

Дополнительное обсуждение и измерения: `https://forum.arduino.cc/t/zs-042-ds3231-rtc-module/268862/39`.

## S-013 — WS2812B / Worldsemi

Источник: `https://world-semi.com/ws2812-family/`

Текущая таблица Worldsemi для WS2812B указывает 12 мА × 3 канала для современных вариантов. Старые WS2812B часто рассчитываются консервативно как до 60 мА на LED при полном RGB. Так как ревизия нашей ленты неизвестна, силовую часть рассчитывать по более тяжёлому сценарию и ограничивать ток программно.

## S-014 — пользовательские уточнения 2026-09-06

Пользователь сообщил:

- два круглых корпуса не имеют рабочего белого света и используются только для ARDU;
- основной свет детской — отдельные точечные светильники на первой клавише;
- вторая клавиша полностью отключает ARDU;
- нужен один БП на два синхронных кольца;
- приложение пишется заново;
- предыдущий прототип приложения собирался в MIT App Inventor;
- в наличии 5 м WS2812B;
- 72,5 см фактической ленты содержат 44 LED;
- внутренний кантик одного корпуса имеет длину 71,5 см.

На дополнительных фото:

- `100 µF 10 V` читается непосредственно на электролите;
- дисковый конденсатор `103` соответствует 10 нФ.

## S-015 — MIT App Inventor: экспорт исходников

Официальная инструкция MIT App Inventor: `https://appinventor.mit.edu/explore/ai2/share`

Проект экспортируется как `.aia` через пункт `Projects/File → Export selected project (.aia) to my computer`. Этот файл содержит исходный проект, включая Designer/Blocks, и пригоден для импорта и анализа.

## S-016 — CR2032 является первичной литиевой батареей

Источник производителя Panasonic Energy: `https://energy.panasonic.com/na/business/products/lithium/coin-cr-standard/models/CR2032`

CR2032 относится к серии **Primary Lithium Batteries**. Поэтому штатную зарядную цепь DS3231-модуля нельзя оставлять включённой при установленной CR2032. Если нужен заряд, требуется отдельный совместимый перезаряжаемый элемент и корректный режим зарядки; для ARDU это не требуется.

## S-017 — Ardublock2.aia / LumpV3.1

Пользовательский исходный проект MIT App Inventor, загружен 2026-09-06.

Файл был распакован и проанализирован напрямую. Подтверждены:

- `Screen1.scm/.bky`;
- `Obichniy.scm/.bky`;
- `Nochnik.scm/.bky`;
- `FON.scm/.bky`;
- `Disco.scm/.bky`.

Из исходников извлечены реальные названия режимов, подрежимы, параметры UI и фактические старые Bluetooth-команды. Подробный разбор: `08_Исходники_и_референсы/Ardublock2_РАЗБОР.md`.

## S-018 — опубликованная таблица управления ColorMusic

Источник: `https://alexgyver.ru/colormusic/`.

На странице проекта опубликована таблица режимов 1–9 с назначением параметров ←→, ↑↓ и подрежимов #. Она использована для проверки соответствия старого `Disco.bky` исходному ColorMusic.

Зафиксированная адаптация: ColorMusic №7 (постоянный цвет / плавная смена / радуга) в ARDU переносится в отдельный раздел фоновых эффектов, потому что он не зависит от звука.

## S-019 — измерение YP-8 2026-09-13

Пользовательское измерение мультиметром на фактическом модуле YP-8/AMS1117-3.3.

Условия: вход YP-8 подключён к 5-вольтовой линии от S-30-5 через макетную плату; ESP8266 и другая нагрузка к VOUT не подключены.

Результат: `VOUT = 3,31 В`.

Вывод: выход без нагрузки соответствует требуемому уровню 3,3 В. Это не заменяет проверку просадки, нагрева и стабильности при активном Wi‑Fi ESP8266.

## S-020 — измерение S-30-5 2026-09-13

Пользовательское измерение мультиметром на фактическом БП S-30-5.

Условия: измерение низковольтной линии `V+ / V-`; существенной рабочей нагрузки нет, YP-8 подключён, но его VOUT не нагружен.

Результат: `5,01 В`.

Вывод: выход S-30-5 без существенной нагрузки соответствует номиналу 5 В. Требуется отдельная проверка под рабочей нагрузкой и контроль нагрева.

## S-021 — YP-8 с подключённым ESP8266 2026-09-13

Пользовательское измерение мультиметром на линии питания фактической белой платы ESP8266 от YP-8.

Условия: ESP8266 подключён к выходу YP-8; вход YP-8 питается от S-30-5.

Результат: `VCC ESP8266 = 3,28 В`; ранее без нагрузки на YP-8 было `3,31 В`.

Вывод: наблюдаемая статическая просадка составляет около `0,03 В`, поэтому базовое питание ESP по уровню напряжения проходит. Активный Wi‑Fi, пиковые просадки, отсутствие перезагрузок и нагрев YP-8 этим измерением не подтверждены.


## S-022 — loopback-тест Nano USB↔UART 2026-09-19

Пользовательский стендовый тест фактической Arduino Nano.

Условия: Nano подключена по USB; `RESET → GND`, `D0/RX ↔ D1/TX`; Serial Monitor `115200`, без участия ESP8266.

Результат: отправленный тестовый текст возвращается в Serial Monitor.

Вывод: USB-UART часть Nano и выбранный COM-порт работают в loopback. Возможность прошивки ESP8266 через этот мост остаётся отдельной проверкой после стабилизации питания ESP.


## S-023 — измерения питания YP-8/ESP8266 2026-09-19

Пользовательские измерения мультиметром при подключённом фактическом ESP8266.

Результаты: `VIN YP-8 = 4,7 В`; `VOUT YP-8 = 3,30 В`; `VCC ESP8266 = 3,10 В`.

Вывод: YP-8 удерживает 3,3 В на собственном выходе под текущей нагрузкой. Падение около `0,20 В` возникает между YP-8 и ESP8266, поэтому требуется проверить/сократить соединения и контакты. Пониженные `4,7 В` на входе YP-8 требуют отдельного измерения непосредственно на клеммах S-30-5 при той же нагрузке.


## S-024 — S-30-5 при подключённом ESP8266 2026-09-19

Пользовательское измерение мультиметром непосредственно на винтовых клеммах фактического S-30-5 при подключённом через YP-8 ESP8266.

Результат: `V+ / V- = 5,00 В`.

Вывод: при текущей небольшой нагрузке S-30-5 удерживает 5 В. Ранее измеренные `4,7 В` на VIN YP-8 и `3,10 В` на VCC ESP обусловлены падением в последующей низковольтной разводке/контактах, а не просадкой самого БП на его клеммах.


## S-025 — измерения питания относительно S-30-5 V- 2026-09-20

Пользовательские измерения мультиметром при подключённом ESP8266. Чёрный щуп оставался на `S-30-5 V-`, красным последовательно измерялись точки цепи.

Результаты: `S-30-5 V+ ≈ 5,0 В`; `YP-8 VIN+ ≈ 4,7 В`; `YP-8 VOUT+ ≈ 3,50 В`; `ESP VCC ≈ 3,25 В`.

Интерпретация: эти показания являются потенциалами относительно `S-30-5 V-`, поэтому `3,50 В` на `YP-8 VOUT+` нельзя трактовать как фактическое напряжение YP-8 между `VOUT/GND`, пока не измерен потенциал локального `YP-8 GND`. Следующая проверка — `YP-8 GND` и `ESP GND` относительно `S-30-5 V-`.


## S-026 — падение по GND-тракту 2026-09-20

Пользовательские измерения при подключённом ESP8266 и чёрном щупе мультиметра на `S-30-5 V-`.

Результаты: `YP-8 GND = 0,8 В`; `ESP GND = 1,5 В`.

Интерпретация: общий низковольтный возврат имеет недопустимо большое падение напряжения. Это согласуется с нестабильными/противоречивыми измерениями питания ESP и указывает на плохой контакт, высокое сопротивление Dupont/макетной шины или разрыв секции шины. До пересборки силовой разводки прошивку и Wi‑Fi ESP8266 не выполнять.


## S-027 — контроль питания после пересборки 2026-09-20

Пользовательские измерения мультиметром после пересборки низковольтной цепи питания ESP8266 прямыми короткими соединениями.

Результаты: `S-30-5 = 5,0 В`; `YP-8 VIN = 5,0 В`; `YP-8 VOUT = 3,3 В`; `ESP VCC = 3,3 В`.

Вывод: ранее наблюдавшаяся большая просадка была связана с макетной/контактной разводкой GND/питания. После её исключения статическое питание ESP8266 соответствует требуемому уровню. Проверка активного Wi‑Fi остаётся обязательной.


## S-028 — проверка делителя Nano→ESP RX 2026-09-20

Пользовательская проверка мультиметром и фото фактических резисторов.

Подтверждено: каждый резистор имеет маркировку `коричневый–зелёный–красный–золотой`, то есть `1,5 кОм ±5%`. После исправления соединения верхнее плечо делителя измерено около `1,5 кОм`, нижнее плечо от узла ESP RX до GND — около `3,0 кОм`.

Вывод: резистивная топология делителя 5 В→около 3,3 В собрана правильно; необходима финальная проверка напряжения при включённом Nano USB-UART.


## S-029 — фактические уровни Nano→ESP 2026-09-20

Пользовательские измерения мультиметром при подключённом Nano USB↔UART-мосте и ESP8266.

Результаты: `Nano D0 = 4,95 В`; после делителя `ESP RX = 3,36 В`; `ESP VCC = 3,20 В`.

Вывод: делитель 1,5 кОм / 3,0 кОм обеспечивает корректный уровень логической HIGH на RX ESP8266. Питание 3,20 В находится внутри документированного диапазона 3,0–3,6 В, но окончательная пригодность YP-8 должна подтверждаться при активной радионагрузке.


## S-030 — первая попытка прошивки ESP8266 2026-09-20

Пользовательский лог Arduino IDE/esptool.py v3.0.

Подтверждено: `ESP8266EX`, Wi‑Fi feature, crystal `26 MHz`, stub успешно загружен и запущен, flash автоматически определена как `4 MB`. Ошибка возникает уже при записи первого сжатого блока: `Failed to write compressed data to flash after seq 0 (result was C100)`.

В документации/issue esptool код `0xC1` соответствует ошибке checksum полученных данных; типичная первая мера — уменьшить скорость serial upload и исключить шум/плохие соединения UART. Источник: espressif/esptool issue #182 и описание serial protocol esptool.


## S-031 — повторная запись на 57600, 2026-09-20

Пользовательский лог Arduino IDE/esptool.py v3.0.

Результат: после ручного reset в bootloader чип `ESP8266EX` и flash `4 MB` успешно определены, stub запущен; запись дошла до `15%`, затем завершилась `Failed to write compressed data to flash after seq 1 (result was C100)`.

Вывод: вход в bootloader исправен; ошибка воспроизводится уже во время передачи/записи и стала возникать позже при меньшей скорости. Следующий диагностический шаг — ещё уменьшить upload speed и отдельно контролировать питание ESP под записью.


## S-032 — esptool 0107 при загрузке stub, 2026-09-20

Пользовательский лог: после установки `100 µF 10 V` у ESP8266 чип `ESP8266EX` определяется, затем при `Uploading stub...` возникает `Failed to write to target RAM (result was 0107)`.

Официальная документация esptool трактует `0107` как timeout при записи в RAM и рекомендует проверять стабильность serial-соединения, драйвер/USB-UART, сокращать путь Host→ESP и при необходимости использовать другой адаптер. Источник: Espressif esptool Troubleshooting.


## S-033 — штатные 1 кОм UART Arduino Nano

Официальная схема Arduino Nano V3.3 (`NanoV3.3_sch.pdf`) показывает резисторную сборку RP1A/RP1B по `1 кОм` между FT232RL и линиями RX/TX ATmega/разъёма. Пользователь отдельно указал наличие соответствующего штатного `1 кОм` на используемой Nano/совместимой плате.

Следствие для стенда: при использовании Nano как USB↔UART-моста этот резистор входит в путь PC→ESP RX и должен учитываться при расчёте согласования. Для рабочего пути ATmega D1/TX→ESP RX он не является последовательным элементом источника сигнала.


## S-034 — крупные фото UART-обвязки Nano 2026-09-20

Пользовательские крупные фото используемой Arduino Nano-совместимой платы.

По маркировке непосредственно на экземпляре видны два SMD `151` (=150 Ом) рядом с 16-выводным USB-UART-чипом и четыре SMD `102` (=1 кОм) возле индикаторов `RX/TX/POW/L` на лицевой стороне.

Вывод: типовая схема другой Nano не должна автоматически переноситься на этот экземпляр. Наличие и номинал последовательного резистора именно в пути USB-UART→D0 требуют прозвонки/измерения на фактической плате.


## S-035 — upload без внешнего делителя, 2026-09-20

Пользовательский лог esptool.py v3.0: при `Upload Speed = 57600` и временном прямом соединении Nano D0→ESP RX запись дошла до `30%` и завершилась `Failed to write compressed data to flash after seq 3 (result was C100)`.

Интерпретация: по сравнению с прежним внешним делителем 1,5 кОм / 3,0 кОм линия стала устойчивее, что указывает на чувствительность к импедансу/фронтам programmer UART. Прямое соединение не является допустимым финальным решением из-за 5-вольтового уровня D0.


## S-036 — первая успешная полная прошивка ESP8266 2026-09-20

Пользовательский лог Arduino IDE / esptool.py v3.0.

Условия успешной попытки: `Upload Speed = 115200`, локальный электролит `100 µF 10 V` между ESP VCC/GND, внешний делитель programmer RX временно отсутствовал, ручной reset при `Connecting...`.

Результат: `ESP8266EX`, flash `4MB`; запись 0–100%; `Wrote 269712 bytes ...`; `Hash of data verified`.

Вывод: физический ESP8266, flash и базовый Nano USB↔UART-путь способны выполнить полную прошивку. Поскольку одновременно были изменены питание и UART-согласование, причина предыдущих ошибок не изолирована. Прямой 5-вольтовый D0→ESP RX не является допустимой финальной схемой.


## S-037 — запуск тестового скетча ESP8266 2026-09-20

Пользовательский результат Serial Monitor после обычного boot ESP8266.

Условия: `GPIO0 = HIGH`, ESP перезапущен после успешной прошивки; Serial Monitor `115200`.

Результат: `ARDU ESP8266 OK`.

Вывод: пользовательская прошивка корректно стартует из flash на физическом модуле. Следующий отдельный тест — активная Wi‑Fi-нагрузка.


## S-038 — повторяемая 100% прошивка ESP8266 2026-09-20

Пользовательский стендовый результат: ESP8266 прошивается до `100%` через Nano USB↔UART на `115200`, если внешний делитель отсутствует, на питании ESP установлен локальный `100 µF 10 V`, `GPIO0 = LOW`, а при `Connecting...` выполняется краткий ручной reset `RST → GND`.

Отдельно подтверждено: без ручного reset соединение не начинается; при возврате делителя `1,5 кОм / 3,0 кОм` upload снова завершается ошибкой.

Инженерный вывод: ручной reset является обязательной частью текущей стендовой процедуры, а безопасное согласование PC→ESP RX ещё не закрыто. Прямой 5-вольтовый TX не считать допустимым финальным решением.


## S-039 — AlexGyver ColorMusic: питание WS2812B

Официальная страница проекта: `https://alexgyver.ru/colormusic/`.

В FAQ проекта указано: электролитический конденсатор на питание ленты — минимум `1000 µF`, рабочее напряжение не ниже `6,3 V`.

## S-040 — AlexGyver WS2812 guide: DATA и фильтрация питания

Официальный гайд: `https://alexgyver.ru/ws2812_guide/`.

Подтверждено:
- DATA: при возможной подаче сигнала на обесточенную ленту ставить последовательный резистор `200–500 Ом`;
- GND Arduino и ленты обязательно соединять;
- на питание микроконтроллера рекомендован электролит около `470 µF` при общем шумном питании;
- на питание ленты — более ёмкий электролит `1000–2200 µF`;
- адресную ленту не питать через Arduino при токе выше допустимого USB/платы.

Для ARDU это означает: `1000 µF` ставится параллельно +5V/GND ленты и **не заменяет** DATA-резистор.


## S-041 — маркировка электролита 1000 µF

Пользовательское чтение маркировки фактического компонента: `1000 µF 6.3 V`.

Вывод: подходит для 5-вольтовой линии WS2812B и соответствует минимальному напряжению, указанному в схеме/рекомендациях ColorMusic.


## S-042 — AlexGyver MAX9814: базовое подключение к Arduino

Официальный урок AlexGyver: `https://alexgyver.ru/lessons/max9814-mic/`.

Подтверждено: MAX9814 питается от `3..5 V`; `Out` подключается к аналоговому входу Arduino; `Gain` без подключения = 60 dB, на GND = 50 dB, на VCC = 40 dB; `AR` можно оставить неподключённым для базового режима. Автор отдельно описывает внешний AREF 3,3 В как опциональный способ увеличить разрешение измерения, а не обязательное условие первого запуска.

Для первого ARDU-теста принято прямое `Out→A0` при стандартном ADC reference Nano, без внешнего AREF; после измерения фактического диапазона решим, нужен ли AREF/иная обработка.


## S-043 — измерения MAX9814 на физическом стенде

Данные владельца 2026-09-20:
- `MIC AVG=249 MIN=61 MAX=413 P2P=352 SAMPLES=251`;
- `MIC AVG=252 MIN=130 MAX=415 P2P=285 SAMPLES=251`.

Средний ADC-код около 250 согласуется с маркированным на модуле DC Offset `1.25V` при стандартной опоре ADC Nano около 5 В. Для оценки реакции на кратковременный звук 50-мс snapshot признан недостаточным; добавлен 1-секундный capture `MIC1S`.


## S-044 — MAX9814: 1-секундные quiet/loud измерения

Физический стенд, 2026-09-20:
- тишина: `AVG=252 MIN=108 MAX=417 P2P=309 SAMPLES=4998`;
- громкий звук/хлопок: `AVG=307 MIN=0 MAX=886 P2P=886 SAMPLES=4995`.

Вывод: MAX9814 исправен и чувствителен к звуку; при `Gain` floating (60 dB) громкий сигнал достигает нижней границы 10-битного ADC (`0`), поэтому требуется уменьшить усиление до 50 dB и повторить тест.


## S-045 — MAX9814: аномальный результат после перемычки Gain→GND

Физический стенд, 2026-09-20:
- `MIC1S AVG=0 MIN=0 MAX=5 P2P=5 SAMPLES=4993`;
- `MIC1S AVG=0 MIN=0 MAX=16 P2P=16 SAMPLES=4992`.

Это резко отличается от предыдущего рабочего состояния `AVG≈252` и требует проверки фактического контакта Gain/Out. До повторной идентификации пина 50 dB не считать проверенным.


## S-046 — MAX9814: восстановление после снятия Gain→GND

Физический стенд, 2026-09-20, при шуме и `Gain` снова floating: `MIC1S AVG=278 MIN=0 MAX=508 P2P=508 SAMPLES=4996`.

Вывод: модуль и A0 работоспособны; предыдущее падение выхода к нулю связано с конкретной попыткой подключения Gain, а не с постоянной неисправностью MAX9814.


## S-047 — крупное фото MAX9814, распиновка конкретного модуля

Фото владельца 2026-09-20 подтверждает подписи контактного ряда слева направо: `AR`, `Out`, `Gain`, `Vdd`, `GND`. На фото видны рабочие подключения: `Out` — жёлтый, `Vdd` — зелёный, `GND` — синий; `AR` и `Gain` свободны.


## S-048 — MAX9814: физические измерения при Gain=50 dB

Данные владельца 2026-09-20:
- тишина: `MIC1S AVG=250 MIN=85 MAX=360 P2P=275 SAMPLES=5000`;
- громкий звук: `MIC1S AVG=260 MIN=0 MAX=507 P2P=507 SAMPLES=996`.

Вывод: правильное подключение Gain к GND подтверждено по восстановившемуся DC-offset около 250 ADC, однако громкий сигнал всё ещё клиппует по нижней границе. Следующий штатный вариант платы — Gain к Vdd (40 dB).


## S-048 — MAX9814 50 dB: сравнение при включённой/выключенной ленте

Данные физического стенда 2026-09-20:
- спокойное состояние: `AVG=250 MIN=85 MAX=360 P2P=275 SAMPLES=5000`;
- шум, лента включена: `AVG=260 MIN=0 MAX=507 P2P=507`;
- шум, лента выключена: `AVG=257 MIN=0 MAX=506 P2P=506 SAMPLES=4991`.

Разница между шумовыми окнами с лентой и без неё пренебрежимо мала для текущего теста; 50 dB всё ещё достигает нижнего rail ADC.


## S-049 — AlexGyver ColorMusic v2.10: алгоритм VU/M01

Официальный репозиторий: `https://github.com/AlexGyver/ColorMusic`, файл `firmware/colorMusic_v2.10/colorMusic_v2.10.ino`.

Для первых двух VU-режимов исходник делает 100 `analogRead`, берёт максимальный отсчёт, отсекает шум через `LOW_PASS`, переводит диапазон в 0..500, применяет степень `EXP=1.4`, сглаживание `SMOOTH=0.3`, медленное среднее `averK=0.006` и `MAX_COEF=1.8` для автоподстройки шкалы. Автокалибровка VU берёт максимум из 200 измерений тишины и задаёт `LOW_PASS = quietMax + LOW_PASS_ADD`, где `LOW_PASS_ADD=13`.

ARDU M01 использует эти же принципы, но адаптирован под один моно-микрофон A0 и кольцо 43 LED.

## S-050 — MAX9814 40 dB, физический стенд

Данные владельца 2026-09-20 при `Gain→Vdd`:
- тишина: `MIC1S AVG=250 MIN=205 MAX=282 P2P=77 SAMPLES=4997`;
- громкий звук: `MIC1S AVG=250 MIN=0 MAX=503 P2P=503 SAMPLES=4999`.

Вывод для M01: положительный пик уверенно отделяется от шума, 40 dB принимается как рабочий Gain первого VU-режима; отрицательный rail clipping экстремального хлопка оставить предметом отдельной проверки перед FHT/частотными режимами.


## S-051 — M01, физическая калибровка и рабочая диагностика

Данные владельца 2026-09-20:
- `CAL QUIET_MAX=260 LOW_PASS=273`;
- `AUDIO PEAK=276 LOW_PASS=273 LEVEL=2.6 FILTERED=17.8 AVG=63.4 MAX=114.2 PAIRS=3`;
- визуальная работа M01 подтверждена владельцем.

Эти данные закрывают первый музыкальный режим на физическом кольце и дают фактический порог тишины для текущего MAX9814 40 dB.


## S-052 — KobaProduction AvrFHT

Репозиторий: `https://github.com/KobaProduction/AvrFHT`.

`library.properties`: версия `1.1.1`, `architectures=avr`. README описывает AvrFHT как модификацию устаревшей ArduinoFHT/Open Music Labs с изменённой архитектурой и оптимизациями памяти.

API, нужный ARDU:
- `FHT_SAMPLES_N` задаёт размер выборки;
- `fht_input[]` сохраняется;
- отдельные headers `fht_window.h`, `fht_reorder.h`, `fht_mag_log.h`;
- преобразование вызывается `fht_transform()`;
- логарифмический результат — `fht_log_out[]`.

## S-053 — AlexGyver ColorMusic содержит старую FHT

Официальный репозиторий `https://github.com/AlexGyver/ColorMusic`, путь `libraries/FHT/` содержит `FHT.h`, reorder/cas/hann tables и examples. Поэтому утверждение «у AlexGyver не было FHT» неверно; она была вложена в репозиторий проекта и не обязана отображаться как отдельная Library Manager библиотека.


## S-054 — первая AvrFHT-калибровка на железе

Данные владельца 2026-09-20:
- `CALF DC=914 QUIET_MAX=32 SPECTR_LOW_PASS=35`;
- `FREQ LOW=0 MID=0 HIGH=0 PEAK=0 LOW_PASS=35` (два раза).

Поскольку ранее тот же MAX9814 устойчиво давал DC около 250 ADC, эти FHT-данные не принимаются как физический результат. Диагностический код изменён так, чтобы DC offset измерять на стандартной скорости ADC и отдельно проверять командой `ADC`.


## S-055 — AvrFHT повторный стендовый результат

Данные владельца 2026-09-20:
- `CALF DC=946 QUIET_MAX=33 SPECTR_LOW_PASS=36`;
- `FREQRAW LOW=19 MID=27 HIGH=19 PEAK=27`;
- `FREQRAW LOW=19 MID=19 HIGH=19 PEAK=19`.

Результат DC остаётся несовместимым с предыдущими прямыми измерениями MAX9814 на A0 (около 250 ADC), поэтому спектр пока считается диагностически невалидным.


## S-056 — MAX9814/FHT, нулевой вход после повторного подключения Gain

Физический стенд 2026-09-21:
- `CALF DC=0 QUIET_MAX=19 SPECTR_LOW_PASS=22`;
- `FREQRAW LOW=0 MID=0 HIGH=0 PEAK=0` два раза.

До восстановления прямого ADC около прежних ~250 эти данные не использовать как характеристику AvrFHT или спектра.


## S-057 — прямой ADC после подтверждения актуальной прошивки

Физический стенд 2026-09-21:
- `CMDS PING STATUS ADC CALF FREQ FREQRAW HELP`;
- `ADC AVG=0 MIN=0 MAX=13`.

Следствие: текущий A0 практически у нулевого rail; спектральные результаты до восстановления OUT/A0 невалидны.


## S-058 — прямые напряжения MAX9814, физический стенд

Измерения владельца 2026-09-21:
- `MAX9814 Vdd→GND = 5,09 В`;
- `MAX9814 Out→GND = 1,24 В`.

Модуль имеет нормальное питание и ожидаемое выходное DC-смещение; текущий нулевой ADC на Nano возникает после точки Out.


## S-059 — напряжение непосредственно на Nano A0

Физический стенд 2026-09-21: `Nano A0→Nano GND = 0,15 В` при `MAX9814 Out→GND = 1,24 В`.

Разница почти 1,1 В на прямой сигнальной линии подтверждает аппаратную проблему между модулем и входом Nano либо нагрузку A0.


## S-060 — восстановление напряжения на Nano A0

Физический стенд 2026-09-21: после исправления провода `MAX9814 Out→Nano A0` измерено `Nano A0→GND = 1,25 В`.

Это подтверждает восстановление аналоговой линии и соответствует ожидаемому DC bias MAX9814.


## S-061 — первый валидный AvrFHT спектр на физическом стенде

Данные владельца 2026-09-21 после ремонта `Out→A0`:
- `ADC AVG=250 MIN=233 MAX=264`;
- `CALF DC=250 QUIET_MAX=61 SPECTR_LOW_PASS=64`;
- тишина: `FREQRAW LOW=19 MID=19 HIGH=16 PEAK=19`;
- звук/музыка: `FREQRAW LOW=64 MID=30 HIGH=19 PEAK=64`.

Вывод: аналоговый вход и AvrFHT работают; спектр реагирует на звук. На данном тестовом фрагменте LOW доминирует, поэтому отдельная проверка MID/HIGH ещё нужна.


## S-062 — FHT gate слишком высок при абсолютном максимуме

Данные владельца 2026-09-21:
- `CALF DC=258 QUIET_MAX=58 SPECTR_LOW_PASS=61`;
- серия `FREQ` — только нули;
- `FREQRAW LOW=37 MID=19 HIGH=19 PEAK=37`.

Поскольку raw peak=37 ниже gate=61, нулевой gated-результат объясняется порогом. Для ARDU CALF адаптирован: вместо абсолютного максимума 100 кадров используется 90-й процентиль максимумов +3. Это сохраняет принцип ColorMusic «измеренный шум + запас», но исключает влияние редких выбросов MAX9814.


## S-063 — оригинальный тракт FHT AlexGyver ColorMusic v2.10

Официальные источники: `AlexGyver/ColorMusic`, `firmware/colorMusic_v2.10/colorMusic_v2.10.ino`, и страница `https://alexgyver.ru/colormusic/`.

Подтверждено:
- `SOUND_R A2` — вход VU;
- `SOUND_R_FREQ A3` — отдельный вход частот, в коде прямо отмечен `через кондер`;
- `FHT_N=64`, логарифмический FHT output;
- при `POTENT=0` используется внутренняя опора 1.1 В;
- ADC prescaler устанавливается в 32;
- spectrum auto-low-pass: 100 FHT кадров, поиск абсолютного максимума по bins 2..31, затем `SPEKTR_LOW_PASS = max + LOW_PASS_FREQ_ADD`, где `LOW_PASS_FREQ_ADD=3`;
- `fullLowPass()` гасит ленту, ждёт 500 мс, выполняет калибровку, ждёт ещё 500 мс и возвращает яркость;
- официальная инструкция требует калибровать шум при поставленной на паузу музыке и рекомендует сохранение порогов в EEPROM.

Вывод для ARDU: текущий прямой MAX9814→A0 с DEFAULT AREF и программным DC subtraction не является точной копией оригинального частотного входа ColorMusic. P90 — эксперимент ARDU, не исходный алгоритм.


## S-064 — официальные версии AlexGyver ColorMusic

Официальная страница `https://alexgyver.ru/colormusic/` содержит changelog: v1.1, v2.0, v2.2, v2.3, v2.4, v2.5, v2.6, v2.6.1, v2.6.2, v2.7, v2.8, v2.10. В списке компонентов указан микрофонный модуль MAX9814. Отдельная официальная ревизия под MAX4466 на странице не найдена.

## S-065 — ColorMusic FireEdition и MAX4466

Community thread: `https://community.alexgyver.ru/threads/cvetomuzyka-na-arduino-fireedition.4683/`.

FireEdition авторства Slenk основана на разработках technotrasher и имеет собственную линейку (2.0, 2.4, 2.5, 2.5.1, 2.6, 2.7, 3.1). Поддерживает MIC/LINE и отдельные `SOUND_R_MIC` / `SOUND_R_MIC_FREQ` входы.

В обсуждении исходного проекта technotrasher 28.12.2020 указал план использовать MAX4466 по совету Diman. В FireEdition-потоке пользователь подтвердил наличие MAX4466; Diman отметил отсутствие AGC у MAX4466, регулируемый больший выходной диапазон и рекомендовал отличающуюся от MAX9814 обвязку. Это относится к community-модификациям и не является официальной версией AlexGyver.


## S-066 — поздние официальные прошивки ColorMusic в репозитории AlexGyver

Официальный репозиторий `AlexGyver/ColorMusic` содержит текущую папку `firmware/colorMusic_v2.10/` и старые официальные ревизии, включая `colorMusic_v2.7` и `colorMusic_v2.8` в `firmware/Old versions/`. README проекта документирует изменения до v2.7; наличие v2.8/v2.10 подтверждается самим деревом официального репозитория и исходниками.


## S-067 — vendored FHT и официальные поздние ColorMusic

Источник: официальный `AlexGyver/ColorMusic`, pinned commit `a3f94de053d431f15282c2f67681b77ab66c30ae`.

В ARDU сохранены:
- исходная `libraries/FHT/` вместе с примерами;
- upstream LICENSE;
- `colorMusic_v2.7.ino`, `colorMusic_v2.8.ino`, `colorMusic_v2.9.ino`, `colorMusic_v2.10.ino`;
- каталог точных имён прошивочных папок и файлов.

Это убирает зависимость компиляции музыкального тракта от сторонней AvrFHT и сохраняет исходный API поздней официальной ColorMusic.


## S-068 — конфликт legacy FHT SCALE с современным FastLED

Физическая компиляция владельца 2026-09-21 показала, что `FHT.h` из ColorMusic содержит `#define SCALE 1`; современный FastLED использует идентификатор `SCALE` в fixed-point типах и gamma LUT, что даёт серию ошибок `expected unqualified-id before numeric constant`.

Compatibility fix сделан в ARDU-скетче, а не в upstream-копии FHT: после `#include <FHT.h>` выполняется `#undef SCALE`, затем подключается `FastLED.h`.


## S-069 — аппаратная проверка сборки original FHT

Отчёт владельца 2026-09-21: после внесения `#undef SCALE` загрузка `nano_fht_test.ino` успешно завершена на реальной Arduino Nano.

Это подтверждает компиляцию и Upload оригинальной vendored FHT в текущем окружении ARDU.


## S-070 — runtime original FHT и рассогласование startup DC

Физический стенд 2026-09-21:
- `HELP -> CMDS PING STATUS ADC CALF FREQ FREQRAW HELP`;
- `STATUS FW=FHTTEST MIC_GAIN=40DB SPECTR_LOW_PASS=40 LIB=AlexGyver_FHT FHT_N=64 DC=390 ADC_REF=DEFAULT ...`;
- `ADC AVG=251 MIN=242 MAX=258`.

Текущий A0 исправен, но startup DC=390 не соответствует установившемуся входу. Для следующего теста добавлена пауза 500 ms; CALF возвращён к оригинальной схеме ColorMusic `max + 3`.


## S-071 — уточнение физического состояния A0 при DC=390

Сообщение владельца 2026-09-21: в прошлом тесте провод с A0 отвалился. Следовательно, ранее полученный `STATUS ... DC=390` был снят при ненадёжном/разомкнутом аналоговом входе и не должен использоваться как характеристика исправного MAX9814→A0.
