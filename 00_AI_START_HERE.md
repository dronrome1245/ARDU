# Нейросеть: начни отсюда

Этот файл — обязательная точка входа для любой нейросети в новом чате по проекту ARDU.

## 1. Что это за проект

ARDU — проект двух синхронных круглых потолочных светильников на WS2812B для светомузыки, фоновых эффектов, ночника и будильника-рассвета с управлением со смартфона по Wi‑Fi.

Основной белый свет детской комнаты отдельный и в ARDU не входит.

Исходная техническая база — ColorMusic AlexGyver, но проект адаптируется под фактически имеющиеся у владельца компоненты. Цель — не покупать новые платы без необходимости.

## 2. Главный принцип

Проектируем от реального наличия:

`инвентаризация → идентификация → проверка характеристик → архитектура → питание → распиновка → прошивка → управление → BOM → закупка только недостающего → стенд → интеграция в лампы`.

Инвентаризация уже закрыта. Сейчас проект находится на этапе измерений питания и первого стенда.

## 3. Что читать перед работой

В каждом новом чате читать в таком порядке:

1. `00_AI_START_HERE.md`;
2. `STATUS.md` — канонический текущий статус;
3. `DECISIONS.md` — принятые и заменённые решения;
4. `HISTORY.md` — фактически выполненные действия;
5. `SOURCES.md` — источники и фактические материалы;
6. `AGENTS.md` — постоянные правила;
7. `02_КОМПОНЕНТЫ_BOM.md` и `02_Наши_детали/ИНВЕНТАРЬ.md`, если задача связана с электроникой/закупками;
8. `04_Прошивка/РЕЖИМЫ_РАБОТЫ.md`, если задача связана с функциями лампы;
9. `05_WiFi_и_приложение/ФУНКЦИОНАЛЬНАЯ_МОДЕЛЬ.md` и `API_V1.md`, если задача связана с приложением/Wi‑Fi;
10. профильные файлы конкретной задачи.

Старые файлы `01_СТАТУС_ПРОЕКТА.md`, `02_СЛЕДУЮЩИЙ_ШАГ.md` и `03_СОСТОЯНИЕ_ПРОЕКТА.yaml` сохраняются для совместимости, но `STATUS.md` имеет приоритет как единый текущий статус.

## 4. Иерархия истины

При противоречии:

1. последнее явное решение владельца проекта;
2. `STATUS.md`;
3. более поздняя запись в `DECISIONS.md`;
4. актуальный профильный документ;
5. старые материалы, ранние схемы и прежние чаты.

Отменённые решения не удалять: помечать `ЗАМЕНЕНО` и сохранять причину.

## 5. После каждого существенного шага

До завершения ответа проверить необходимость обновления:

- `STATUS.md`;
- `DECISIONS.md`;
- `HISTORY.md`;
- `SOURCES.md`;
- `02_КОМПОНЕНТЫ_BOM.md`;
- `02_Наши_детали/ИНВЕНТАРЬ.md`.

Если изменился практический следующий шаг, синхронизировать также старые файлы handoff: `01_СТАТУС_ПРОЕКТА.md`, `02_СЛЕДУЮЩИЙ_ШАГ.md`, `03_СОСТОЯНИЕ_ПРОЕКТА.yaml`.

Нельзя писать «зафиксировано в проекте», если запись в GitHub фактически не выполнена при наличии доступа.

## 6. Факт, предположение и решение

Всегда различать:

- **ФАКТ** — читается с маркировки, подтверждён фото, измерением, документацией или исходным файлом;
- **ПРЕДПОЛОЖЕНИЕ** — вероятная идентификация/характеристика, требующая проверки;
- **РЕШЕНИЕ** — явно выбранный вариант проекта.

Если маркировка на фото читается не полностью, не придумывать номинал.

## 7. Текущая рабочая архитектура

`мобильное приложение → домашний Wi‑Fi → ESP8266 → UART → Arduino Nano → MAX9814 / DS3231 / два WS2812B-кольца`

- ESP8266 — сетевой шлюз;
- Nano — реальное управление светом, анализом звука и RTC;
- кольца имеют отдельные DATA-линии D6/D7 и показывают синхронный кадр;
- HC-05 остаётся только резервной деталью, не частью финальной архитектуры.

## 8. Новый интерфейс управления

Старый `Ardublock2.aia` (`LumpV3.1`) уже разобран.

Главный вывод: старое Bluetooth-приложение используем как функциональный референс, но не сохраняем его протокол и схему соединения.

Новый внешний API:

- локальный HTTP;
- Station как основной режим Wi‑Fi;
- SoftAP как резерв первичной настройки/восстановления;
- приложение при подключении читает актуальное состояние устройства.

## 9. Функциональные разделы

Новая лампа имеет четыре раздела:

1. Светомузыка;
2. Фон;
3. Ночник;
4. Будильник/рассвет.

Цветная подсветка ColorMusic перенесена из музыкальных режимов в фоновые эффекты.

Подробный состав режимов: `04_Прошивка/РЕЖИМЫ_РАБОТЫ.md`.

## 10. Покупки

Статусы BOM:

- ✅ `ЕСТЬ` — подтверждено наличие;
- 🔵 `ПРОВЕРИТЬ` — вещь есть, но нужно подтвердить номинал/исправность/совместимость;
- 🟢 `ДОКУПИТЬ` — подтверждённо нужно для принятой архитектуры;
- 🟡 `ПОСЛЕ ИЗМЕРЕНИЙ/ТЕСТА` — покупать только после контрольного теста;
- ⚪ `ОПЦИОНАЛЬНО` — не требуется для базовой версии;
- ⛔ `НЕ НУЖНО ДЛЯ ARDU` — компонент может быть исправен, но в текущей архитектуре не нужен.

Не расширять список покупок до выполнения ближайших измерений YP-8/S-30-5 и определения физической потолочной разводки.

## 11. Текущий следующий шаг

1. измерить YP-8;
2. измерить S-30-5;
3. выяснить проводку/расстояние/место общего БП;
4. утвердить окончательный BOM;
5. прошить ESP8266;
6. собрать первый стенд ESP8266 ↔ Nano → одно кольцо;
7. добиться `PING/PONG` и постоянного цвета;
8. затем подключать MAX9814 и M01 VU-градиент.
