# Порядок реализации прошивки ARDU

Дата: 2026-09-06

Цель — не переносить весь ColorMusic одним большим скетчем, а собирать систему проверяемыми слоями.

## Этап FW-0 — базовый Nano

- определить финальную конфигурацию пинов;
- Serial diagnostic;
- неблокирующий главный цикл;
- структура настроек;
- сохранение настроек после стабилизации структуры.

Критерий: Nano стабильно стартует и отвечает на диагностические команды.

## Этап FW-1 — одно кольцо без музыки

- FastLED;
- одно кольцо WS2812B;
- F01 постоянный цвет;
- яркость;
- программный лимит тока.

Критерий: кольцо стабильно работает от S-30-5 на стенде.

## Этап FW-2 — два синхронных кольца

- второй DATA-выход;
- одинаковый кадр на A/B;
- тест обрыва/отключения одного кольца;
- проверка питания обоих колец.

Критерий: оба корпуса логически синхронны и не зависят от DATA-цепочки друг друга.

## Этап FW-3 — UART с ESP8266

- `PING/PONG`;
- чтение состояния;
- установка F01 по команде;
- подтверждение `OK/ERR`.

Критерий: приложение/HTTP пока не нужно; UART проверяется через ESP или Serial.

## Этап FW-4 — MAX9814

- чтение A0;
- измерение покоя;
- калибровка шума;
- вывод диагностического уровня;
- проверка чувствительности.

Критерий: уровень звука устойчиво отличается от шума помещения.

## Этап FW-5 — первый музыкальный режим

- M01 VU градиент;
- плавность;
- яркость активных/фоновых LED;
- реакция на калибровку.

Критерий: светомузыка реагирует на музыку без ложного постоянного свечения.

## Этап FW-6 — частотный анализ

Переносить из ColorMusic только после рабочего M01:

- M03 5 полос;
- M04 3 полосы;
- M05 подрежимы частот;
- M08 бегущие частоты;
- M09 спектр.

Для AVR/Nano проверить совместимость FHT и нагрузку вместе с двумя лентами и UART.

## Этап FW-7 — фоновые эффекты

- F02 плавная смена цвета;
- F03 бегущая радуга;
- автоперебор.

## Этап FW-8 — DS3231 и рассвет

- чтение RTC;
- синхронизация времени;
- хранение будильника;
- автономный рассвет;
- остановка рассвета;
- восстановление после перезапуска.

## Этап FW-9 — ночник

- цвет/яркость;
- при необходимости расписание.

## Этап FW-10 — полный набор и стабилизация

- M02;
- M06 стробоскоп с отдельным ограничением;
- обработка ошибок;
- долговременный тест;
- финальный лимит тока;
- сохранение настроек;
- версия протокола.

## Главное правило

Новый режим добавляется только после того, как предыдущий уровень системы имеет воспроизводимый тест. Не объединять одновременно отладку питания, Wi‑Fi, FHT и двух лент.


## Текущий статус реализации — 2026-09-20

- `FW-0`: **ПРОЙДЕН**. Mini-USB Nano прошивается как `ATmega328P (Old Bootloader)`; `PING→PONG`, `STATUS` подтверждены на железе.
- `FW-1`: код подготовлен в `04_Прошивка/nano_fw1/nano_fw1.ino`; следующий тест — компиляция с FastLED, затем одно кольцо на D6.
- До физического FW-1 не подключать второе кольцо, ESP8266 или MAX9814.

- Первая физическая проверка: компиляция FW-0 прошла, upload пока не прошёл (`resp=0x41`). До смены bootloader/Processor сначала полностью освободить Nano D0/D1 от ESP и убрать `RESET→GND`.

- `FW-1` компиляция: **ПРОЙДЕНА**. Следующий критерий — физически получить стабильный F01 на одном канале D6 при лимите 500 мА.

- `FW-1`: **ПРОЙДЕН НА ЖЕЛЕЗЕ**. Один канал D6, 43 LED, F01, смена цвета и Serial status подтверждены.
- `FW-2`: код подготовлен в `04_Прошивка/nano_fw2/nano_fw2.ino`; ближайший тест — компиляция без подключения второго кольца.


## Уточнённый порядок после FW-1 — решение владельца 2026-09-20

Физический `FW-2` с двумя кольцами переносится на более поздний этап. До этого вся функциональность отлаживается на одном кольце D6. D7 считается зеркальным резервным выходом и отдельной логики не получает.

Текущая последовательность:

`FW-1 одно кольцо/F01 → однокольцевая интеграция следующих подсистем → полная стабилизация одного кольца → подключение D7/второго кольца как зеркала → проверка питания двух колец`.

`nano_fw2` сохраняется как будущий тестовый инструмент и не является следующим обязательным шагом.

- Однокольцевой F01: **ПОЛНОСТЬЮ ПРОЙДЕН** (`OFF/ON/BRIGHT/COLOR/STATUS`).
- Следующий однокольцевой слой: MAX9814 на A0. Подготовлен `04_Прошивка/nano_mic_test/nano_mic_test.ino`; сначала компиляция/загрузка, затем команда `MIC` в тишине и при звуке.

- `FW-4` MAX9814: **ПРОЙДЕН**. Для M01 принят `Gain→Vdd` (40 dB), AR floating; тишина и громкий звук устойчиво различаются.
- `FW-5` M01: создан `04_Прошивка/nano_m01/nano_m01.ino`. Следующий критерий — компиляция/загрузка, `CAL` в тишине и визуальная реакция VU-градиента на реальную музыку.

- `FW-5` M01 VU-градиент: **ПРОЙДЕН НА ЖЕЛЕЗЕ**. CAL и визуальная реакция на музыку подтверждены.
- `FW-6a` FHT diagnostic: создан `04_Прошивка/nano_fht_test/nano_fht_test.ino`; сначала компиляция и CALF/FREQ без реализации светового M03.

- FHT dependency для ARDU: `AvrFHT` 1.1.1 или совместимая более новая AVR-версия. Тестовый код уже адаптирован; старую вложенную `ColorMusic/libraries/FHT` отдельно ставить не требуется.

- `FW-6a` AvrFHT basic: **РАБОТАЕТ НА ЖЕЛЕЗЕ** — ADC bias, CALF и изменение FREQRAW на звук подтверждены. До M03/M04 осталось проверить noise gate (`FREQ`) и устойчивое разделение трёх диапазонов.

- `FW-6a`: базовый FHT работает; noise-gate CALF адаптирован под MAX9814: вместо абсолютного максимума используется P90 максимумов 100 кадров +3. Следующий критерий — после новой калибровки тишина подавляется, а обычная музыка проходит gate.


- FHT-зависимость музыкального тракта: **оригинальная FHT из официального AlexGyver ColorMusic**, vendored в `04_Прошивка/libraries/FHT/`. AvrFHT больше не основная зависимость. Перед следующим аппаратным тестом скопировать именно эту папку в Arduino libraries и подтвердить Verify/Compile `nano_fht_test.ino`.

- Original FHT integration: **COMPILE + UPLOAD PASSED** на фактической Nano с `#undef SCALE` compatibility shim. Следующий критерий — runtime `HELP/STATUS/ADC`.

- `FW-6a`: original AlexGyver FHT + original ColorMusic `CALF=max+3` теперь дают валидный порог (`36`) и raw peak до `82`. Следующий критерий — gated `FREQ`: тишина должна подавляться, реальный звук должен проходить порог.

- `FW-6a` noise gate: **PASSED ON HARDWARE** — тишина подавляется при `LOW_PASS=36`, звуковые события проходят (`LOW=38/66`). До реализации M03/M04 остаётся отдельно подтвердить MID и HIGH.

- `FW-6a` diagnostic на A0: **ПОЛНОСТЬЮ ПРОЙДЕН** — original FHT, CALF=max+3, noise gate и LOW/MID/HIGH подтверждены контролируемыми 2/5/8 кГц. Следующий аппаратный этап — перенести музыкальный вход на финальную логику ColorMusic v2.10 A2/A3 и повторить сокращённую проверку.

- После полного FW-6a на A0 создан отдельный `nano_audio_v210_test`; не изменять уже пройденный A0 diagnostic. Следующий шаг: Verify/Upload нового A2/A3-скетча, затем собрать `AUDIO_V210_WIRING.md` и выполнить STATUS→ADC2→ADC3→CALF→FREQ.

- Исправление 2026-09-22: A2/A3 собирать без резистора A2→GND. Точная цепь официальной схемы: MAX9814 Out→C1→A2, A2→103→A3. Предыдущий 9,4 kΩ pull-down отменён.


- Первый A2/A3 runtime: **PASSED** — `STATUS` подтверждает A2/A3 + INTERNAL 1,1 V; `ADC2=123 (105..154)`, `ADC3=745 (726..779)`, насыщения нет. Следующий минимальный шаг: `CALF` в тишине, затем несколько `FREQ` в тишине; только после этого 2/5/8 кГц.


- A2/A3 `CALF`: **PASSED** — `QUIET_MAX=37`, `SPECTR_LOW_PASS=40`. Следующий критерий: несколько `FREQ` подряд в тишине должны подтвердить работу gate.


- A2/A3 gated `FREQ` в тишине: **PASSED** — три последовательных результата `0/0/0` при `LOW_PASS=40`. Следующий критерий: регрессия 2/5/8 кГц на финальном A3.


- A2/A3 tone regression: **НЕ ПРОЙДЕН** — 2/5/8 кГц после gate дают `0/0/0`. Следующий минимальный тест: `FREQRAW` при непрерывном 2 кГц; проводку и код до этого не менять.


- A2/A3 `FREQRAW` при 2 кГц: `LOW=37 MID=19 HIGH=19 PEAK=37`, то есть peak совпадает с `QUIET_MAX=37` и не проходит gate 40. Следующий тест: при том же 2 кГц выполнить `ADC2` и `ADC3`; если A2 растёт, а A3 нет, локализовать ветвь 103/A3.


- ADC под 2 кГц подтверждает прохождение сигнала: A2 размах 124 против 49 в тишине; A3 размах 66 против 53. Полного обрыва A3 нет. Следующий тест — `FREQRAW` на 5 и 8 кГц для проверки частотной зависимости ветви 103.


- A3 raw 2/5/8 кГц: **НЕ РАЗЛИЧАЮТСЯ** — во всех случаях `37/19/19`. Обнаружено отличие от A0 baseline: тот тест вычитал DC программно. Диагностический скетч расширен командами `ADC3FAST` и `FREQRAWZ`; следующий минимальный шаг после Upload — обе команды при 2 кГц.


- `ADC3FAST` при тоне: **SATURATED 1023/1023/1023**; `FREQRAWZ=0` является следствием полного клиппинга. Официальная инструкция ColorMusic предупреждает, что `POTENT=0`/1.1 V работает не при любом уровне сигнала. Следующий тест: `ADC3FAST` в тишине; если также saturated — перейти на штатную регулируемую AREF с имеющимся B10K.


- Диагностика A3 расширена: `ADC2FAST` сравнивает основной вход при prescaler=32; `ADC3TRACE` выводит первые 16 быстрых выборок A3. Следующий тест выполнять в тишине. Если только A3 уезжает в 1023, менять FHT/порог не нужно — локализовать DC-привязку/источник A3.


- Fast ADC в тишине: A2 и A3 оба насыщаются; `A3TRACE` = 16×1023. Следующий аппаратный тест — временная DC-привязка A2 и A3 к GND по 9,4 kΩ на каждый вход. После включения сначала повторить `ADC2FAST/ADC3FAST/ADC3TRACE`; к FHT переходить только если saturation исчез.


- Временная ветка сравнения: MAX4466. Созданы отдельные wiring/test files. Первый критерий — отсутствие saturation на A2/A3 при fast ADC с DEFAULT AREF; затем CALF и 2/5/8 kHz. MAX9814 baseline не удалять и не переписывать.


- MAX4466 baseline: fast ADC не saturate, но все значения около 7–8 counts при DEFAULT 5V. До CALF проверить мультиметром VCC и OUT непосредственно на модуле; ожидаемый типичный OUT≈VCC/2.


- MAX4466: прямое измерение `VCC=5,1 V`, `OUT≈0 V`. FHT остановлен. Следующий критерий — изолированный модуль (только VCC/GND) должен показать OUT около VCC/2; иначе диагностировать сам MAX4466.


- MAX4466 изоляционный тест: **PASSED** — `VCC=5,1 V`, `OUT=2,55 V`. Модуль исправен. Следующий шаг: прямой OUT→A2, без резисторов и A3; проверить `ADC2/ADC2FAST`.


- MAX4466 direct OUT→A2: slow/fast ADC стабильны (~244/~230), saturation нет. Следующий критерий — физически измерить OUT и A2 при подключении; к FHT переходить только после согласования напряжения и ADC.


- MAX4466 direct A2: мультиметр подтверждает 2,55 V на самом A2, но diagnostic ADC даёт ~230–244. Следующий шаг — отдельный `nano_adc_sanity` без FHT/FastLED/prescaler. Только после этого продолжать MAX4466/FHT.


- MAX4466 comparison: **ОСТАНОВЛЕН** — изолированный модуль при VCC≈5,1 V даёт OUT≈0,1 V вместо ожидаемого порядка VCC/2. Не использовать его для решений по FHT до подтверждения исправности.


- **D-040 / основной путь:** вернуться к подтверждённому MAX9814 Out→A0 с DEFAULT AREF и software DC subtraction. Новый `nano_audio_single_test` объединяет проверку VU и FHT на одном входе. Следующий pass: STATUS/ADC/CALV/VU/CALF/FREQ, затем 2/5/8 kHz.


- D-040 первый runtime: A0 **РАБОТАЕТ** (`AVG=237, 225..250, P2P=25`), но startup `DC=467` невалиден. Следующий шаг: `CALF` в тишине должен заново измерить DC и spectrum noise; затем `STATUS/FREQ`.


- D-040 `CALF`: **PASSED** — `DC=234`, `QUIET_MAX=33`, `SPECTR_LOW_PASS=36`, согласовано с `ADC AVG=237`. Следующий критерий: `FREQ` в тишине, затем 2/5/8 kHz.


- 2026-09-24: D-040 single-input A0/FHT **ПРОЙДЕН НА ЖЕЛЕЗЕ**. LOW/MID/HIGH подтверждены; пограничный HIGH на 8 кГц переносится в настройку реального режима со сглаживанием.
- 2026-09-24: создан `04_Прошивка/nano_m03/nano_m03.ino` — первый режим FW-6/M03 «5 полос» на одном кольце D6. Код объединяет проверенный A0/FHT frontend с частотной логикой ColorMusic v2.10. **Следующий критерий: Verify/Compile; M03 ещё не считается пройденным.**

- 2026-09-24: M03 **COMPILE + UPLOAD PASSED** на фактической Nano. Следующий критерий — runtime `STATUS/ADC/CALF/BANDS`, затем визуальная проверка 2/4–5/8 кГц на одном D6-кольце.
- 2026-09-25: M03 **ПОЛНОСТЬЮ ПРОЙДЕН НА ЖЕЛЕЗЕ** на одном D6-кольце: 5 зон, тишина→полное гашение при `BACKGROUND 0`, 2/5/8 кГц→LOW/MID/HIGH, реальная музыка проверена. Следующий слой — M04 «3 полосы».

- 2026-09-25: создан `04_Прошивка/nano_m04/nano_m04.ino` — M04 «3 полосы» на том же проверенном A0/FHT frontend. Отрисовка: HIGH → MID → LOW по трём последовательным третям 43-LED кольца. Default background = 0. Следующий критерий — Verify/Compile, затем Upload/STATUS на физической Nano.

- 2026-09-25: M04 **COMPILE + UPLOAD PASSED** на фактической Nano. Startup `FW=M04 MODE=M04 ... BACKGROUND=0 DC=250 CAL=NO` подтверждён. Следующий критерий — `ADC`, затем валидный `CALF` и визуальная проверка трёх зон.

- 2026-09-25: M04 `ADC` **PASSED**: `AVG=249 MIN=243 MAX=256 P2P=13`. Следующий критерий — `CALF` в тишине.

- 2026-09-25: M04 `CALF` **PASSED**: `DC=251 QUIET_MAX=43 SPECTR_LOW_PASS=46`; тишина подавляется, реакция на реальные короткие звуки есть. Следующий критерий — 2/5/8 кГц + визуальная проверка трёх зон.

- 2026-09-25: M04 на постоянном синусе визуально затухает после адаптации среднего — это соответствует исходной flash-логике ColorMusic v2.10 и не считается отказом. Следующий критерий: после визуального гашения при продолжающемся 2/5/8 кГц проверить `BANDS` → LOW/MID/HIGH.

- 2026-09-25: M04 band classification **PASSED**: 2/5/8 кГц корректно определяются как LOW/MID/HIGH. Последний критерий M04 — визуальная проверка трёх третей на реальной музыке и гашение в паузе; только после этого переходить к M05.

- 2026-09-25: M04 **ПОЛНОСТЬЮ ПРОЙДЕН НА ЖЕЛЕЗЕ**: три зоны на реальной музыке и полное гашение в паузе подтверждены.
- 2026-09-25: создан `04_Прошивка/nano_m05_r2/nano_m05_r2.ino` — M05 «1 полоса» с `THREE/LOW/MID/HIGH`, на том же A0/FHT frontend. Следующий критерий — Verify/Compile.

- 2026-09-25: первая компиляция M05 выявила конфликт `Submode::LOW/HIGH` с Arduino macros `LOW/HIGH`; исправлено на внутренние `LOW_ONLY/MID_ONLY/HIGH_ONLY` без изменения Serial-команд. Следующий критерий — повторный Verify/Compile.

- 2026-09-25: после compile-fix M05 введена явная ревизия **R2**. Актуальный файл: `04_Прошивка/nano_m05_r2/nano_m05_r2.ino`; startup/STATUS содержат `R2/REV=2`. Следующий критерий — Verify/Compile R2.

- 2026-09-25: M05 R2 **COMPILE + UPLOAD PASSED**. Следующий критерий — startup/STATUS с явным `R2 / REV=2`, затем ADC/CALF и проверка подрежимов.

- 2026-09-25: M05 R2 startup/STATUS **PASSED**; `FW=M05 REV=2`, `SUBMODE=THREE`, `BACKGROUND=0`, `DC=250`, `CAL=NO`. Следующий критерий — `ADC`, затем `CALF`.

- 2026-09-25: M05 R2 runtime `ADC=10 (6..28)` при предыдущем startup `DC=250` — hardware/frontend fault gate. ADC-код совпадает с M04; `CALF` не выполнять. Сначала восстановить физический A0 до ~240–260 counts.

- 2026-09-26: M05 R2 A0 восстановлен: `ADC AVG=250 MIN=241 MAX=262 P2P=21`. Далее одним пакетом: `CALF` + проверка `THREE/LOW/MID/HIGH`.

- 2026-09-26: M05 R2 после reset показал invalid `DC=1000`; тест подрежимов не засчитан. Создан **M05 R3** с `DC_OK` и отказом `CALF` вне `120..400`. Следующий критерий — Verify/Upload R3, затем batch `STATUS/ADC/CALF/STATUS`, потом `THREE/LOW/MID/HIGH`.

- 2026-09-26: M05 R3 guard **PASSED**: invalid DC=26/25 отклонён, затем валидные CALF=250/250/251 приняты; reset в серии не было. Подрежимы принимаются. Перед следующим режимом — короткий stability-check A0 из-за intermittent DC-провалов.

- 2026-09-26: M05 visual logic **PASSED**, but UART commands corrupt only while active FastLED output. Created **M05 R4** with `FASTLED_ALLOW_INTERRUPTS=1` + 5 ms serial RX guard. Gate before M08: prove rapid Serial commands work while ring is continuously lit and LED output stays stable.

- 2026-09-26: M05 R4 **ПОЛНОСТЬЮ ПРОЙДЕН НА ЖЕЛЕЗЕ**: visual submodes + DC guard + active-ring UART stress без ошибок. R4 FastLED/UART fix становится baseline следующих режимов.
- 2026-09-26: создан `04_Прошивка/nano_m08_r1/nano_m08_r1.ino` — M08 running frequencies, `THREE/LOW/MID/HIGH`, `SPEED 1..255`, center index 21 для 43 LED. Следующий критерий — Verify/Upload, ADC/CALF, visual/speed regression.

- 2026-09-26: M08 R1 **ПОЛНОСТЬЮ ПРОЙДЕН НА ЖЕЛЕЗЕ**: submodes, speed и active-ring UART regression подтверждены.
- 2026-09-26: создан `04_Прошивка/nano_m09_r1/nano_m09_r1.ino` — M09 spectrum analyzer, 20 bins, HUE_START/HUE_STEP, inherited DC/UART guards. Следующий критерий — Verify/Upload, ADC/CALF, visual spectrum regression.

- 2026-09-26: M09 R1 **ПОЛНОСТЬЮ ПРОЙДЕН НА ЖЕЛЕЗЕ**. Этап **FW-6 закрыт** на одном D6-кольце.
- 2026-09-26: начат FW-7; создан `04_Прошивка/nano_f02_r1/nano_f02_r1.ino` — F02 smooth color cycle с HUE/SAT/SPEED/BRIGHT и inherited UART fix. Следующий критерий — Verify/Upload + visual parameter regression.

- 2026-09-26: F02 R1 **ПОЛНОСТЬЮ ПРОЙДЕН НА ЖЕЛЕЗЕ**: HUE/SAT/SPEED и active-ring UART regression подтверждены.
- 2026-09-26: создан `04_Прошивка/nano_f03_r1/nano_f03_r1.ino` — F03 running rainbow с `SPEED`, `STEP10`, `HUE`, inherited UART fix. Следующий критерий — Verify/Upload + visual speed/step regression.

- 2026-09-26: F03 R1 **ПРОЙДЕН НА ЖЕЛЕЗЕ**.
- 2026-09-26: создан `04_Прошивка/nano_ambient_r1/nano_ambient_r1.ino` — интеграция F01/F02/F03 + NEXT/PREV + AUTO/PERIOD. Следующий критерий — ручная и автоматическая смена эффектов при активном кольце без UART errors; после pass FW-7 можно закрывать.

- 2026-09-26: AMBIENT R1 **ПРОЙДЕН**, этап **FW-7 закрыт**.
- 2026-09-26: начат FW-8 минимальным RTC sanity layer: `04_Прошивка/nano_rtc_r1/nano_rtc_r1.ino` + `RTC_DS3231_WIRING.md`. До подачи 5 В с установленной CR2032 обязательно подтвердить удаление `201`. После этого критерий: I²C/clock/OSF/SET/battery retention.

- 2026-09-26: RTC R1 **ПРОЙДЕН НА ЖЕЛЕЗЕ**: I²C, ход времени, temperature, SET и OSF подтверждены; D-017/CR2032 retention подтверждены владельцем.
- 2026-09-26: создан `04_Прошивка/nano_alarm_r1/nano_alarm_r1.ino` — следующий FW-8 layer: persisted daily alarm trigger без LED dawn. Критерий — настройки переживают reset, событие `ALARM_TRIGGER` возникает ровно один раз в заданную минуту.

- 2026-09-26: ALARM R1 **ПРОЙДЕН НА ЖЕЛЕЗЕ**: EEPROM persistence + single daily trigger подтверждены. Повтор после `ALARM CLEARLAST` в той же минуте ожидаем.
- 2026-09-26: создан `04_Прошивка/nano_dawn_r1/nano_dawn_r1.ino` — реальный sunrise renderer + alarm start + manual accelerated test. Следующий критерий: `DAWN TEST 20`, STOP, parameter regression, затем alarm-triggered 1-minute dawn.

- 2026-09-26: DAWN R1 **ПРОЙДЕН НА ЖЕЛЕЗЕ**: renderer, параметрический test, STOP и alarm-start подтверждены.
- 2026-09-26: создан `04_Прошивка/nano_dawn_r2/nano_dawn_r2.ino` — RTC-based recovery активного рассвета после reset без periodic EEPROM writes. Следующий критерий — reset во время `DAWN TEST 30`, HOLD/STOP persistence и alarm-dawn reset regression.

- 2026-09-26: DAWN R2 **ПРОЙДЕН НА ЖЕЛЕЗЕ**: TEST/ALARM reset recovery, HOLD/STOP persistence подтверждены. Этап **FW-8 закрыт**.
- 2026-09-26: начат FW-9; создан `04_Прошивка/nano_night_r1/nano_night_r1.ino` — persistent constant-HSV nightlight без расписания. Следующий критерий — визуальный HUE/SAT/BRIGHT test + ON/OFF persistence после reset.
